/**
 * Plan 타입 정의 (하위 호환성 유지)
 * 
 * @deprecated 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새로운 코드는 @/lib/types/plan에서 직접 import하세요.
 * 
 * 이 파일은 lib/types/plan/index.ts의 모든 타입을 re-export합니다.
 */

// 모든 타입을 re-export
export * from "./plan/index";

