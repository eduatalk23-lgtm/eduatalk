# 다크/라이트 모드 컴포넌트 검토 및 수정

**작업 일시**: 2025-12-22  
**목적**: 프로젝트의 각종 컴포넌트들의 다크/라이트 모드 지원 상태를 검토하고 개선

---

## 📋 작업 개요

프로젝트의 다크/라이트 모드 가이드를 확인하고, 주요 컴포넌트들을 검토하여 CSS 변수 기반 유틸리티를 사용하도록 수정했습니다.

### 다크 모드 가이드 원칙

1. **CSS 변수 기반 유틸리티 우선 사용**
   - `lib/utils/darkMode.ts`에서 제공하는 CSS 변수 기반 유틸리티 사용
   - `textPrimaryVar`, `bgSurfaceVar`, `borderDefaultVar` 등

2. **불필요한 `dark:` 클래스 제거**
   - CSS 변수는 자동으로 다크 모드에 대응하므로 `dark:` 클래스가 불필요
   - 예외: hover 상태 등 CSS 변수만으로 처리하기 어려운 경우

3. **하드코딩된 색상 클래스 제거**
   - `bg-white`, `text-gray-900` 등 하드코딩된 색상 클래스를 CSS 변수 기반으로 변경

---

## 🔧 수정된 컴포넌트

### 1. Card 컴포넌트 (`components/molecules/Card.tsx`)

**문제점**:
- `text-text-primary`, `text-text-secondary` 같은 잘못된 클래스 사용
- CSS 변수 기반 유틸리티 미사용

**수정 내용**:
```tsx
// 수정 전
<h3 className="text-h2 text-text-primary dark:text-text-primary">
<p className="text-body-2 text-text-secondary dark:text-text-secondary">

// 수정 후
import { textPrimaryVar, textSecondaryVar } from "@/lib/utils/darkMode";
<h3 className={cn("text-h2", textPrimaryVar)}>
<p className={cn("text-body-2", textSecondaryVar)}>
```

**효과**:
- CSS 변수 기반으로 자동 다크 모드 지원
- 불필요한 `dark:` 클래스 제거

---

### 2. Select 컴포넌트 (`components/atoms/Select.tsx`)

**문제점**:
- `bg-white` 하드코딩으로 다크 모드 미지원

**수정 내용**:
```tsx
// 수정 전
"w-full rounded-lg border bg-white transition-base appearance-none cursor-pointer",

// 수정 후
import { bgSurfaceVar } from "@/lib/utils/darkMode";
"w-full rounded-lg border transition-base appearance-none cursor-pointer",
bgSurfaceVar,
```

**효과**:
- 다크 모드에서 자동으로 배경색 변경
- CSS 변수 기반으로 일관된 스타일 관리

---

### 3. Dialog 컴포넌트 (`components/ui/Dialog.tsx`)

**문제점**:
- 닫기 버튼에서 중복된 `dark:` 클래스 사용
- CSS 변수 기반으로 개선 가능

**수정 내용**:
```tsx
// 수정 전
"text-[var(--text-secondary)] dark:text-[var(--text-tertiary)]",
"hover:text-[var(--text-primary)] dark:hover:text-[var(--text-primary)]",

// 수정 후
"text-[var(--text-secondary)]",
"hover:text-[var(--text-primary)]",
```

**효과**:
- 중복된 `dark:` 클래스 제거
- CSS 변수 자동 대응으로 코드 간소화

---

### 4. DropdownMenu 컴포넌트 (`components/ui/DropdownMenu.tsx`)

**문제점**:
- `bg-white dark:bg-secondary-900` 하드코딩

**수정 내용**:
```tsx
// 수정 전
"bg-white dark:bg-secondary-900",

// 수정 후
import { bgSurfaceVar } from "@/lib/utils/darkMode";
bgSurfaceVar,
```

**효과**:
- CSS 변수 기반으로 자동 다크 모드 지원
- 일관된 배경색 관리

---

### 5. Tabs 컴포넌트 (`components/molecules/Tabs.tsx`)

**문제점**:
- CSS 변수 사용 시 불필요한 `dark:` 클래스 중복 사용

**수정 내용**:
```tsx
// 수정 전
"border-[var(--text-primary)] dark:border-[var(--text-primary)] text-[var(--text-primary)] dark:text-[var(--text-primary)]"
"text-[var(--text-tertiary)] dark:text-[var(--text-tertiary)]"

// 수정 후
"border-[var(--text-primary)] text-[var(--text-primary)]"
"text-[var(--text-tertiary)]"
```

**효과**:
- 불필요한 `dark:` 클래스 제거
- CSS 변수 자동 대응으로 코드 간소화

---

## ✅ 검토 완료된 컴포넌트

다음 컴포넌트들은 이미 다크 모드를 잘 지원하고 있어 수정이 필요하지 않았습니다:

1. **Button** (`components/atoms/Button.tsx`)
   - CSS 변수 기반 유틸리티 사용
   - 모든 variant에 다크 모드 지원

2. **Input** (`components/atoms/Input.tsx`)
   - CSS 변수 기반 유틸리티 사용
   - 다크 모드 완벽 지원

3. **Badge** (`components/atoms/Badge.tsx`)
   - CVA를 사용한 variant 시스템
   - Semantic Colors 활용으로 다크 모드 지원

4. **Label** (`components/atoms/Label.tsx`)
   - CSS 변수 사용 (`text-[var(--text-primary)]`)

5. **ProgressBar** (`components/atoms/ProgressBar.tsx`)
   - CSS 변수 사용
   - 다크 모드 지원

6. **ToggleSwitch** (`components/atoms/ToggleSwitch.tsx`)
   - CSS 변수 사용
   - 다크 모드 지원

7. **Spinner** (`components/atoms/Spinner.tsx`)
   - CSS 변수 사용 (`text-[var(--text-secondary)]`)

8. **EmptyState** (`components/molecules/EmptyState.tsx`)
   - CSS 변수 기반 유틸리티 사용
   - 다크 모드 지원

9. **StatCard** (`components/molecules/StatCard.tsx`)
   - Semantic Colors 활용
   - 다크 모드 완벽 지원

10. **ErrorState** (`components/ui/ErrorState.tsx`)
    - Semantic Colors 활용
    - 다크 모드 지원

11. **LoadingSkeleton** (`components/ui/LoadingSkeleton.tsx`)
    - CSS 변수 사용
    - 다크 모드 지원

---

## 📝 다크 모드 가이드 준수 사항

### ✅ 적용된 원칙

1. **CSS 변수 기반 유틸리티 우선 사용**
   - `textPrimaryVar`, `textSecondaryVar`, `bgSurfaceVar` 등 사용
   - `lib/utils/darkMode.ts`에서 제공하는 유틸리티 활용

2. **불필요한 `dark:` 클래스 제거**
   - CSS 변수는 자동으로 다크 모드에 대응하므로 `dark:` 클래스 불필요
   - 예외: hover 상태 등 CSS 변수만으로 처리하기 어려운 경우

3. **하드코딩된 색상 클래스 제거**
   - `bg-white`, `text-gray-900` 등을 CSS 변수 기반으로 변경

### 📚 참고 자료

- 다크 모드 사용 가이드: `docs/dark-mode-usage-guide.md`
- 다크 모드 구현 문서: `docs/2025-02-02-dark-mode-implementation.md`
- 다크 모드 유틸리티: `lib/utils/darkMode.ts`
- 전역 CSS 변수: `app/globals.css`

---

## 🎯 향후 개선 사항

1. **추가 컴포넌트 검토**
   - `components/ui/` 폴더의 나머지 컴포넌트들
   - `components/forms/` 폴더의 폼 컴포넌트들
   - `components/organisms/` 폴더의 컴포넌트들

2. **페이지 레벨 검토**
   - 각 페이지에서 하드코딩된 색상 클래스 사용 여부 확인
   - CSS 변수 기반 유틸리티로 마이그레이션

3. **일관성 검증**
   - 모든 컴포넌트에서 CSS 변수 기반 유틸리티 사용 확인
   - 린터 규칙 추가 고려

---

**완료 일시**: 2025-12-22  
**관련 파일**:
- `components/molecules/Card.tsx`
- `components/atoms/Select.tsx`
- `components/ui/Dialog.tsx`
- `components/ui/DropdownMenu.tsx`
- `components/molecules/Tabs.tsx`

