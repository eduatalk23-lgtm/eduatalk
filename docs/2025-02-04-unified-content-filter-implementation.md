# 통합 콘텐츠 필터 컴포넌트 구현

**작성일**: 2025-02-04  
**작업자**: AI Assistant  
**상태**: 완료 ✅

---

## 📋 개요

콘텐츠 검색 관련 필터 항목을 통일하고, 모든 페이지에서 일관된 필터를 사용할 수 있도록 통합 필터 컴포넌트를 구현했습니다.

---

## 🎯 목표

1. 학생, 관리자, 콘텐츠 마스터 페이지의 필터 항목 통일
2. 모든 페이지에서 난이도, 정렬 필터 추가
3. 플랜 그룹 생성 시 콘텐츠 마스터 검색에도 동일한 필터 적용

---

## 📦 구현 내용

### 1. 타입 정의 업데이트

**파일**: `lib/data/contentMasters.ts`

- `MasterBookFilters` 타입에 `difficulty`, `sort` 필드 추가
- `MasterLectureFilters` 타입에 `difficulty`, `sort` 필드 추가
- `ContentMasterFilters` 타입에도 동일 필드 추가

### 2. 데이터 조회 함수 확장

**파일**: `lib/data/contentMasters.ts`

- `searchMasterBooks()` 함수에 난이도 필터링 및 정렬 로직 추가
- `searchMasterLectures()` 함수에 난이도 필터링 및 정렬 로직 추가
- 정렬 옵션: `title_asc`, `title_desc`, `difficulty_level_asc`, `difficulty_level_desc`, `created_at_asc`, `created_at_desc`, `updated_at_desc` (기본값)

### 3. 난이도 목록 조회 함수 추가

**파일**: `lib/data/contentMasters.ts`

- `getDifficultiesForMasterBooks()`: 마스터 교재의 난이도 목록 조회
- `getDifficultiesForMasterLectures()`: 마스터 강의의 난이도 목록 조회

### 4. 통합 필터 컴포넌트 생성

**파일**:
- `components/filters/types.ts`: 필터 타입 정의
- `components/filters/UnifiedContentFilter.tsx`: 통합 필터 컴포넌트

**특징**:
- 계층형 필터 구조 (개정교육과정 → 교과 → 과목)
- 콘텐츠 타입별 필터 (출판사/플랫폼)
- 난이도 필터
- 정렬 필터
- 제목 검색

### 5. 페이지별 필터 적용

#### 학생 마스터 콘텐츠 페이지

**파일**:
- `app/(student)/contents/master-books/page.tsx`
- `app/(student)/contents/master-lectures/page.tsx`

**변경사항**:
- `HierarchicalFilter` → `UnifiedContentFilter`로 교체
- 난이도, 정렬 필터 추가
- 필터 옵션 조회 시 난이도 목록 포함
- 검색 함수에 난이도, 정렬 파라미터 전달

#### 관리자 마스터 콘텐츠 페이지

**파일**:
- `app/(admin)/admin/master-books/page.tsx`
- `app/(admin)/admin/master-lectures/page.tsx`

**변경사항**:
- `HierarchicalFilter` → `UnifiedContentFilter`로 교체
- 난이도, 정렬 필터 추가
- 필터 옵션 조회 시 난이도 목록 포함

#### 플랜 그룹 생성 시 콘텐츠 마스터 검색

**파일**:
- `app/(student)/plan/new-group/_components/ContentMasterSearch.tsx`
- `app/(student)/actions/contentMasterActions.ts`

**변경사항**:
- 난이도, 정렬 상태 추가
- 난이도 목록 조회 (API 추가)
- 검색 시 난이도, 정렬 필터 전달
- UI에 난이도, 정렬 드롭다운 추가

### 6. API 엔드포인트 추가

**파일**:
- `app/api/master-books/difficulties/route.ts`: 교재 난이도 목록 조회
- `app/api/master-lectures/difficulties/route.ts`: 강의 난이도 목록 조회

---

## 📊 최종 통일된 필터 항목

| 순서 | 필터 항목 | 필수/선택 | 적용 범위 | 데이터 형식 |
|------|----------|---------|----------|------------|
| 1 | **제목 검색** | 필수 | 모든 페이지 | 텍스트 입력 |
| 2 | **개정교육과정** | 필수 | 모든 페이지 | ID 기반 (계층형) |
| 3 | **교과** | 필수 | 모든 페이지 | ID 기반 (계층형) |
| 4 | **과목** | 필수 | 모든 페이지 | ID 기반 (계층형) |
| 5 | **출판사** | 조건부 | 교재만 | ID 기반 |
| 6 | **플랫폼** | 조건부 | 강의만 | ID 기반 |
| 7 | **난이도** | 필수 | 모든 페이지 | 텍스트 |
| 8 | **정렬** | 필수 | 모든 페이지 | 선택 옵션 |

---

## 🔧 기술 상세

### 정렬 옵션

- `updated_at_desc`: 최신순 (기본값)
- `created_at_desc`: 최신순
- `created_at_asc`: 오래된순
- `title_asc`: 제목 가나다순
- `title_desc`: 제목 역순
- `difficulty_level_asc`: 난이도 낮은순
- `difficulty_level_desc`: 난이도 높은순

### 난이도 필터

- 마스터 콘텐츠에서 실제로 사용되는 난이도 값만 표시
- 테넌트별로 다른 난이도 목록 제공 가능

---

## 📝 주요 변경 파일

### 타입 및 데이터 로직
- `lib/data/contentMasters.ts`
- `lib/types/plan.ts`

### 컴포넌트
- `components/filters/types.ts` (신규)
- `components/filters/UnifiedContentFilter.tsx` (신규)

### 페이지
- `app/(student)/contents/master-books/page.tsx`
- `app/(student)/contents/master-lectures/page.tsx`
- `app/(admin)/admin/master-books/page.tsx`
- `app/(admin)/admin/master-lectures/page.tsx`
- `app/(student)/plan/new-group/_components/ContentMasterSearch.tsx`

### Actions
- `app/(student)/actions/contentMasterActions.ts`

### API
- `app/api/master-books/difficulties/route.ts` (신규)
- `app/api/master-lectures/difficulties/route.ts` (신규)

---

## ✅ 완료된 작업

- [x] 타입 정의 업데이트 (MasterBookFilters, MasterLectureFilters에 difficulty, sort 추가)
- [x] 데이터 조회 함수 확장 (searchMasterBooks, searchMasterLectures에 난이도, 정렬 로직 추가)
- [x] 난이도 목록 조회 함수 추가 (마스터 콘텐츠용)
- [x] 통합 필터 컴포넌트 생성 (UnifiedContentFilter)
- [x] 학생 마스터 콘텐츠 페이지에 난이도, 정렬 필터 추가
- [x] 관리자 마스터 콘텐츠 페이지에 난이도, 정렬 필터 추가
- [x] 플랜 그룹 생성 시 콘텐츠 마스터 검색에 난이도, 정렬 필터 추가
- [x] 문서 작성 및 커밋

---

## 🚀 다음 단계 (선택사항)

- [ ] 학생 콘텐츠 페이지에 통합 필터 적용 (현재는 FilterBar 사용 중)
- [ ] 필터 상태 URL 파라미터로 관리 (브라우저 뒤로가기 지원)
- [ ] 필터 옵션 캐싱 최적화

---

## 📚 참고

- 기존 `HierarchicalFilter` 컴포넌트는 레거시로 남겨두었으나, 새로운 페이지에서는 `UnifiedContentFilter` 사용 권장
- 난이도 필터는 실제 데이터에 존재하는 값만 표시되도록 동적으로 조회
- 정렬 옵션은 페이지별로 기본값을 다르게 설정 가능

