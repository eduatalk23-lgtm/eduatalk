# 관리자 페이지 콘텐츠 관련 텍스트 가이드 준수 점검

**작성일**: 2025-01-03  
**작성자**: AI Assistant  
**점검 범위**: 관리자 페이지의 콘텐츠 관련 페이지

## 📋 점검 개요

관리자 페이지의 콘텐츠 관련 페이지들에 대한 텍스트 가이드 준수 여부를 점검했습니다.

### 점검 대상 페이지

1. **콘텐츠 메타데이터 관리** (`/admin/content-metadata`)
   - 교육과정 계층 관리
   - 플랫폼 관리
   - 출판사 관리
   - 진로 계열 관리

2. **교재 관리** (`/admin/master-books`)
   - 교재 목록
   - 교재 등록
   - 교재 수정

3. **강의 관리** (`/admin/master-lectures`)
   - 강의 목록
   - 강의 등록
   - 강의 수정

---

## 🔍 발견된 문제점

### 1. 알림 시스템 불일치 ⚠️ **중요**

**문제**: 일부 컴포넌트는 `toast`를 사용하고, 일부는 `alert`/`confirm`을 사용합니다.

#### Toast 사용 (권장)
- `CurriculumHierarchyManager.tsx` - `toast.showError()`, `toast.showSuccess()` 사용

#### Alert 사용 (비권장)
- `PlatformsManager.tsx` - `alert()` 사용
- `PublishersManager.tsx` - `alert()` 사용
- `CareerFieldsManager.tsx` - `alert()` 사용
- `SubjectTypesManager.tsx` - `alert()` 사용
- `SubjectCategoriesManager.tsx` - `alert()` 사용
- `SubjectsManager.tsx` - `alert()` 사용
- `CurriculumRevisionsManager.tsx` - `alert()` 사용
- `MasterBookForm.tsx` - `alert()` 사용
- `MasterLectureForm.tsx` - `alert()` 사용

**영향**: 사용자 경험의 일관성이 떨어지며, 프로젝트 가이드라인에 맞지 않습니다.

**권장 사항**:
- 모든 알림을 `ToastProvider`의 `toast.showError()`, `toast.showSuccess()` 등으로 통일
- `confirm()`은 중요한 삭제 확인에만 사용 (Dialog 컴포넌트로 대체 고려)

---

### 2. 버튼 텍스트 불일치

**문제**: 동일한 기능의 버튼에 다른 텍스트를 사용합니다.

| 기능 | 사용되는 텍스트 | 위치 |
|------|---------------|------|
| 새 항목 추가 | `+ 추가` | PlatformsManager, PublishersManager, CareerFieldsManager |
| 새 항목 추가 | `+ 교재 등록` | master-books/page.tsx |
| 새 항목 추가 | `+ 강의 등록` | master-lectures/page.tsx |
| 저장 | `저장` | 모든 폼 |
| 취소 | `취소` | 모든 폼 |
| 수정 | `수정` | 모든 테이블 |
| 삭제 | `삭제` | 모든 테이블 |
| 상세보기 | `상세보기` | master-books/page.tsx, master-lectures/page.tsx |

**권장 사항**:
- 일반적인 추가: `+ 추가` 또는 `추가`
- 특정 항목 추가: `+ {항목명} 등록` (예: `+ 교재 등록`)
- 일관성 유지: 동일 기능은 동일 텍스트 사용

---

### 3. 로딩 메시지 불일치

**문제**: 로딩 상태 표시 문구가 통일되지 않았습니다.

| 현재 사용 문구 | 위치 |
|--------------|------|
| `로딩 중...` | PlatformsManager, PublishersManager, CareerFieldsManager, SubjectTypesManager, SubjectCategoriesManager, CurriculumHierarchyManager, SubjectsManager, CurriculumRevisionsManager |
| `불러오는 중...` | 일부 컴포넌트 |
| `{데이터명}을 불러오는 중...` | 일부 컴포넌트 |

**권장 사항**:
- 간단한 로딩: `로딩 중...` (기본)
- 구체적인 로딩: `{데이터명}을 불러오는 중...` (긴 작업)
- 모든 컴포넌트에서 일관성 유지

---

### 4. 에러 메시지 형식 불일치

**문제**: 에러 메시지 형식이 페이지별로 다릅니다.

#### Toast 사용 시
```typescript
toast.showError("개정교육과정을 불러오는데 실패했습니다.");
toast.showError("데이터를 불러오는데 실패했습니다.");
toast.showError(error instanceof Error ? error.message : "생성에 실패했습니다.");
```

#### Alert 사용 시
```typescript
alert("플랫폼을 불러오는데 실패했습니다.");
alert(error instanceof Error ? error.message : "생성에 실패했습니다.");
```

**발견된 패턴**:
- `{데이터명}을 불러오는데 실패했습니다.`
- `{데이터명}을 불러오는 중 오류가 발생했습니다.`
- `{작업명}에 실패했습니다.`

**권장 사항**:
- 표준 패턴: `{작업명}에 실패했습니다.` (간결)
- 구체적 패턴: `{데이터명}을 불러오는데 실패했습니다.` (명확)
- 에러 객체 활용: `error instanceof Error ? error.message : "기본 메시지"`

---

### 5. 성공 메시지 형식 불일치

**문제**: 성공 메시지 형식이 일관되지 않습니다.

#### Toast 사용 시
```typescript
toast.showSuccess("개정교육과정이 생성되었습니다.");
toast.showSuccess("교과가 수정되었습니다.");
```

**발견된 패턴**:
- `{데이터명}이 생성되었습니다.`
- `{데이터명}이 수정되었습니다.`
- `{데이터명}이 삭제되었습니다.`

**권장 사항**:
- 표준 패턴: `{데이터명}이 {작업}되었습니다.`
- 일관성 유지: 모든 성공 메시지에 동일 패턴 적용

---

### 6. 빈 상태 메시지 불일치

**문제**: 데이터가 없을 때 표시되는 메시지가 다릅니다.

| 현재 사용 문구 | 위치 |
|--------------|------|
| `데이터가 없습니다.` | PlatformsManager, PublishersManager, CareerFieldsManager, SubjectTypesManager, SubjectCategoriesManager, SubjectsManager, CurriculumRevisionsManager |
| `검색 결과가 없습니다` | master-books/page.tsx, master-lectures/page.tsx |
| `등록된 커스텀 콘텐츠가 없습니다.` | ContentListSection.tsx |

**권장 사항**:
- 일반 테이블: `데이터가 없습니다.`
- 검색 결과: `검색 결과가 없습니다.`
- 특정 항목: `{항목명}이 없습니다.` (예: `등록된 교재가 없습니다.`)

---

### 7. 페이지 제목 및 설명 형식 불일치

**문제**: 페이지 제목과 설명 문구가 일관되지 않습니다.

#### 콘텐츠 메타데이터 관리
```tsx
<h1>콘텐츠 메타데이터 관리</h1>
<p>개정교육과정, 학년, 학기, 교과, 과목, 플랫폼, 출판사를 관리합니다.</p>
```

#### 교재 목록
```tsx
<p className="text-sm font-medium text-gray-500">서비스 마스터</p>
<h1 className="text-3xl font-semibold text-gray-900">교재 목록</h1>
<p className="text-sm text-gray-500">
  서비스에서 제공하는 교재를 검색하고 확인하세요.
</p>
```

#### 강의 목록
```tsx
<p className="text-sm font-medium text-gray-500">서비스 마스터</p>
<h1 className="text-3xl font-semibold text-gray-900">강의 목록</h1>
<p className="text-sm text-gray-500">
  서비스에서 제공하는 강의를 검색하고 확인하세요.
</p>
```

**권장 사항**:
- 제목 형식: `{페이지명}` (간결)
- 설명 형식: `{기능 설명}하세요.` 또는 `{기능 설명}합니다.`
- 일관성 유지: 모든 페이지에 동일한 구조 적용

---

### 8. 테이블 헤더 텍스트 불일치

**문제**: 테이블 헤더 텍스트가 일관되지 않습니다.

| 현재 사용 | 권장 |
|---------|------|
| `작업` | `작업` 또는 `관리` |
| `상태` | `상태` |
| `정렬 순서` | `정렬 순서` 또는 `순서` |
| `이름` | `이름` 또는 `항목명` |

**권장 사항**:
- 테이블 헤더는 간결하게
- 동일 의미는 동일 텍스트 사용
- 필요시 도움말 추가

---

### 9. 확인 메시지 형식 불일치

**문제**: 삭제 확인 메시지가 다릅니다.

| 현재 사용 | 위치 |
|---------|------|
| `정말 삭제하시겠습니까?` | PlatformsManager, PublishersManager, CareerFieldsManager, SubjectsManager, CurriculumRevisionsManager |
| `정말 삭제하시겠습니까? 관련된 교과, 과목, 과목구분도 함께 삭제됩니다.` | CurriculumHierarchyManager |
| `정말 삭제하시겠습니까? 관련된 과목도 함께 삭제됩니다.` | CurriculumHierarchyManager |
| `정말 삭제하시겠습니까? 사용 중인 과목이 있으면 삭제할 수 없습니다.` | CurriculumHierarchyManager |

**권장 사항**:
- 기본: `정말 삭제하시겠습니까?`
- 관련 데이터 있을 때: `정말 삭제하시겠습니까? {경고 메시지}`

---

### 10. 검색 관련 텍스트 불일치

**문제**: 검색 관련 메시지가 다릅니다.

| 현재 사용 | 위치 |
|---------|------|
| `총 {개수}개의 {항목}이 검색되었습니다.` | master-books/page.tsx, master-lectures/page.tsx |
| `검색 결과가 없습니다` | master-books/page.tsx, master-lectures/page.tsx |
| `다른 검색 조건으로 시도해보세요.` | master-books/page.tsx, master-lectures/page.tsx |

**권장 사항**:
- 결과 개수: `총 {개수}개의 {항목}이 검색되었습니다.`
- 빈 결과: `검색 결과가 없습니다.`
- 안내 문구: `다른 검색 조건으로 시도해보세요.`

---

## ✅ 텍스트 가이드라인 제안

### 알림 메시지

#### 에러 메시지
```typescript
// 기본 패턴
toast.showError("작업에 실패했습니다.");

// 구체적 패턴
toast.showError("{데이터명}을 불러오는데 실패했습니다.");

// 에러 객체 활용
toast.showError(error instanceof Error ? error.message : "기본 메시지");
```

#### 성공 메시지
```typescript
// 표준 패턴
toast.showSuccess("{데이터명}이 {작업}되었습니다.");
// 예: "교재가 등록되었습니다.", "플랫폼이 수정되었습니다."
```

#### 정보 메시지
```typescript
toast.showInfo("정보 메시지");
```

---

### 버튼 텍스트

| 기능 | 표준 텍스트 |
|------|-----------|
| 새 항목 추가 (일반) | `+ 추가` |
| 새 항목 추가 (특정) | `+ {항목명} 등록` |
| 저장 | `저장` |
| 취소 | `취소` |
| 수정 | `수정` |
| 삭제 | `삭제` |
| 상세보기 | `상세보기` |
| 검색 | `검색` |

---

### 로딩 메시지

```typescript
// 기본 로딩
"로딩 중..."

// 구체적 로딩
"{데이터명}을 불러오는 중..."
```

---

### 빈 상태 메시지

| 상황 | 표준 텍스트 |
|------|-----------|
| 일반 테이블 | `데이터가 없습니다.` |
| 검색 결과 | `검색 결과가 없습니다.` |
| 특정 항목 | `{항목명}이 없습니다.` |

---

### 확인 메시지

```typescript
// 기본 확인
"정말 삭제하시겠습니까?"

// 경고 포함
"정말 삭제하시겠습니까? {경고 메시지}"
```

---

### 페이지 제목 및 설명

```tsx
// 구조
<div>
  <h1 className="text-3xl font-semibold text-gray-900">{페이지명}</h1>
  <p className="text-sm text-gray-500">{기능 설명}</p>
</div>

// 예시
<h1>교재 목록</h1>
<p>서비스에서 제공하는 교재를 검색하고 확인하세요.</p>
```

---

## 📝 개선 권장 사항

### 우선순위 높음 🔴

1. **알림 시스템 통일**
   - 모든 `alert()` → `toast.showError()`, `toast.showSuccess()` 변경
   - `confirm()` → Dialog 컴포넌트 고려 (선택)

2. **에러 메시지 형식 통일**
   - 표준 패턴 적용
   - 에러 객체 활용

3. **성공 메시지 형식 통일**
   - 표준 패턴 적용

### 우선순위 중간 🟡

4. **로딩 메시지 통일**
   - 기본 로딩 메시지 표준화

5. **빈 상태 메시지 통일**
   - 상황별 표준 메시지 적용

6. **페이지 제목/설명 형식 통일**
   - 일관된 구조 적용

### 우선순위 낮음 🟢

7. **버튼 텍스트 통일**
   - 기존 텍스트 대부분 양호, 소수 개선

8. **검색 관련 텍스트 통일**
   - 현재 양호, 일부 세부 조정

---

## 📋 점검 체크리스트

### 콘텐츠 메타데이터 관리

- [ ] CurriculumHierarchyManager: Toast 사용 ✅ (완료)
- [ ] PlatformsManager: Alert → Toast 변경 필요
- [ ] PublishersManager: Alert → Toast 변경 필요
- [ ] CareerFieldsManager: Alert → Toast 변경 필요
- [ ] SubjectTypesManager: Alert → Toast 변경 필요
- [ ] SubjectCategoriesManager: Alert → Toast 변경 필요
- [ ] SubjectsManager: Alert → Toast 변경 필요
- [ ] CurriculumRevisionsManager: Alert → Toast 변경 필요

### 교재 관리

- [ ] MasterBookForm: Alert → Toast 변경 필요
- [ ] MasterBookEditForm: 확인 필요
- [ ] master-books/page.tsx: 텍스트 형식 확인 (양호)

### 강의 관리

- [ ] MasterLectureForm: Alert → Toast 변경 필요
- [ ] MasterLectureEditForm: 확인 필요
- [ ] master-lectures/page.tsx: 텍스트 형식 확인 (양호)

---

## 🔗 관련 파일

### 콘텐츠 메타데이터 관리
- `app/(admin)/admin/content-metadata/page.tsx`
- `app/(admin)/admin/content-metadata/_components/ContentMetadataTabs.tsx`
- `app/(admin)/admin/content-metadata/_components/CurriculumHierarchyManager.tsx`
- `app/(admin)/admin/content-metadata/_components/PlatformsManager.tsx`
- `app/(admin)/admin/content-metadata/_components/PublishersManager.tsx`
- `app/(admin)/admin/content-metadata/_components/CareerFieldsManager.tsx`
- `app/(admin)/admin/content-metadata/_components/SubjectTypesManager.tsx`
- `app/(admin)/admin/content-metadata/_components/SubjectCategoriesManager.tsx`
- `app/(admin)/admin/content-metadata/_components/SubjectsManager.tsx`
- `app/(admin)/admin/content-metadata/_components/CurriculumRevisionsManager.tsx`

### 교재 관리
- `app/(admin)/admin/master-books/page.tsx`
- `app/(admin)/admin/master-books/new/MasterBookForm.tsx`
- `app/(admin)/admin/master-books/[id]/edit/MasterBookEditForm.tsx`

### 강의 관리
- `app/(admin)/admin/master-lectures/page.tsx`
- `app/(admin)/admin/master-lectures/new/MasterLectureForm.tsx`
- `app/(admin)/admin/master-lectures/[id]/edit/MasterLectureEditForm.tsx`

---

## 📌 참고

- 프로젝트 가이드라인: `.cursor/rules/project_rule.mdc`
- Toast 컴포넌트: `components/ui/ToastProvider.tsx`
- Toast 사용 예시: `app/(admin)/admin/content-metadata/_components/CurriculumHierarchyManager.tsx`

---

**다음 단계**: 텍스트 가이드 통일 작업 진행
