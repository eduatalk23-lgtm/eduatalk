# 사이드바 네비게이션 적용 가이드

## 🎯 추천 방안: Route Group으로 이동

### 왜 이 방법이 좋은가?

1. **URL 변경 없음**: Next.js Route Group은 URL에 영향을 주지 않습니다
   - `app/plan/page.tsx` → `/plan` (현재)
   - `app/(student)/plan/page.tsx` → `/plan` (이동 후, URL 동일)

2. **자동 적용**: `(student)/layout.tsx`가 자동으로 적용되어:
   - 사이드바 네비게이션 표시
   - 권한 체크 (student만 접근)
   - Breadcrumbs 표시
   - 모바일 네비게이션 표시

3. **일관성**: 학생 전용 페이지를 한 곳에 모아 관리

4. **코드 변경 최소화**: 파일 이동만으로 해결

## 📦 이동해야 할 디렉토리 목록

다음 디렉토리들을 `app/`에서 `app/(student)/`로 이동:

```
app/
├── plan/              → app/(student)/plan/
├── contents/          → app/(student)/contents/
├── analysis/          → app/(student)/analysis/
├── goals/             → app/(student)/goals/
├── scores/            → app/(student)/scores/
├── schedule/          → app/(student)/schedule/
├── scheduler/         → app/(student)/scheduler/
├── reports/            → app/(student)/reports/
├── report/            → app/(student)/report/
└── blocks/            → app/(student)/blocks/
```

## 🔧 실행 단계

### 1단계: 디렉토리 이동

```bash
# Windows PowerShell
Move-Item app/plan app/(student)/plan
Move-Item app/contents app/(student)/contents
Move-Item app/analysis app/(student)/analysis
Move-Item app/goals app/(student)/goals
Move-Item app/scores app/(student)/scores
Move-Item app/schedule app/(student)/schedule
Move-Item app/scheduler app/(student)/scheduler
Move-Item app/reports app/(student)/reports
Move-Item app/report app/(student)/report
Move-Item app/blocks app/(student)/blocks
```

또는 수동으로 파일 탐색기에서 이동

### 2단계: Import 경로 확인

대부분의 import는 상대 경로이므로 문제없지만, 다음을 확인:

- `@/app/actions/*` → 그대로 유지 (actions는 route group 밖에 있음)
- `@/lib/*` → 그대로 유지
- `@/components/*` → 그대로 유지

### 3단계: 테스트

각 페이지를 방문하여 확인:

- ✅ 사이드바가 표시되는지
- ✅ 권한 체크가 작동하는지 (student가 아니면 `/login`으로 리다이렉트)
- ✅ Breadcrumbs가 표시되는지
- ✅ 모바일 네비게이션도 작동하는지

## ⚠️ 주의사항

### 1. 공유 페이지 처리

만약 다른 역할(admin, parent)도 접근해야 하는 페이지가 있다면:
- 해당 페이지는 route group 밖에 유지
- 또는 별도의 공통 레이아웃 생성

### 2. Actions 파일

`app/actions/` 폴더는 route group 밖에 유지:
- 모든 route group에서 공유
- Import 경로 변경 불필요

### 3. API Routes

`app/api/` 폴더는 route group 밖에 유지:
- 모든 route group에서 공유

## 📊 예상 결과

### 이동 전
```
app/
├── (student)/
│   ├── layout.tsx (사이드바 있음)
│   ├── dashboard/
│   ├── today/
│   └── focus/
├── plan/ (사이드바 없음 ❌)
├── contents/ (사이드바 없음 ❌)
└── ...
```

### 이동 후
```
app/
├── (student)/
│   ├── layout.tsx (사이드바 있음)
│   ├── dashboard/
│   ├── today/
│   ├── focus/
│   ├── plan/ (사이드바 있음 ✅)
│   ├── contents/ (사이드바 있음 ✅)
│   ├── analysis/ (사이드바 있음 ✅)
│   ├── goals/ (사이드바 있음 ✅)
│   ├── scores/ (사이드바 있음 ✅)
│   ├── schedule/ (사이드바 있음 ✅)
│   ├── scheduler/ (사이드바 있음 ✅)
│   ├── reports/ (사이드바 있음 ✅)
│   ├── report/ (사이드바 있음 ✅)
│   └── blocks/ (사이드바 있음 ✅)
```

## 🎉 장점 요약

1. **URL 변경 없음** - 기존 링크/북마크 유지
2. **자동 적용** - 레이아웃이 자동으로 적용됨
3. **권한 체크** - student만 접근 가능
4. **일관성** - 모든 학생 페이지가 동일한 레이아웃
5. **유지보수** - 한 곳에서 관리

## 🔄 대안 (비추천)

### 옵션 2: 공통 레이아웃 생성
- 각 디렉토리에 `layout.tsx` 생성 필요
- 코드 중복 발생
- 유지보수 어려움

### 옵션 3: 개별 레이아웃 적용
- 각 페이지마다 레이아웃 생성
- 코드 중복 최대
- 비효율적

