# 캠프 템플릿 학생 입력 제한 근본 수정

## 📋 작업 개요

캠프 템플릿에서 "학생 입력 허용" 체크박스를 체크하지 않았을 때 (`undefined`)도 입력이 허용되던 근본적인 문제를 수정했습니다.

## 🐛 문제 분석

### 문제 상황

- 캠프 템플릿에서 "학생 입력 허용" 체크박스를 체크하지 않았을 때 (`undefined`)
- 현재 로직은 `undefined`를 입력 허용으로 처리
- 결과적으로 체크박스를 체크하지 않아도 학생이 필드를 수정할 수 있었음

### 원인

`canStudentInput` 함수와 관련 로직에서 `undefined`를 허용으로 처리하고 있었습니다:

```typescript
// 문제가 있던 코드
const fieldValue = lockedFields[fieldName];
// undefined 또는 true이면 입력 가능, false이면 입력 불가
return fieldValue !== false;  // ❌ undefined도 true로 처리됨
```

**의도된 동작**:
- 체크박스를 체크했을 때 (`true`): 학생이 입력 가능 ✅
- 체크박스를 체크하지 않았을 때 (`false` 또는 `undefined`): 학생이 입력 불가 ❌

**실제 동작**:
- 체크박스를 체크했을 때 (`true`): 학생이 입력 가능 ✅
- 체크박스를 체크하지 않았을 때 (`undefined`): 학생이 입력 가능 ❌ (문제!)
- 명시적으로 해제했을 때 (`false`): 학생이 입력 불가 ✅

### 영향받는 파일

1. **Step1 필드** (기본 정보):
   - `app/(student)/plan/new-group/_components/utils/fieldLockUtils.ts`
     - `canStudentInput` 함수
     - `isFieldLocked` 함수

2. **Step2 필드** (시간 설정):
   - `app/(student)/plan/new-group/_components/_features/scheduling/components/ExclusionsPanel.tsx`
   - `app/(student)/plan/new-group/_components/_features/scheduling/components/TimeConfigPanel.tsx`
   - `app/(student)/plan/new-group/_components/_features/scheduling/components/AcademySchedulePanel.tsx`
   - `app/(student)/plan/new-group/_components/_features/scheduling/components/NonStudyTimeBlocksPanel.tsx`

## ✅ 수정 내용

### 1. `fieldLockUtils.ts` - `canStudentInput` 함수 수정

```typescript
// 수정 전
const fieldValue = lockedFields[fieldName];
// undefined 또는 true이면 입력 가능, false이면 입력 불가
return fieldValue !== false;

// 수정 후
const fieldValue = lockedFields[fieldName];
// 명시적으로 true인 경우만 입력 가능, undefined나 false이면 입력 불가
return fieldValue === true;
```

### 2. `fieldLockUtils.ts` - `isFieldLocked` 함수 수정

```typescript
// 수정 전
const fieldValue = lockedFields[allowFieldName as keyof typeof lockedFields];
// 명시적으로 false인 경우만 고정
return fieldValue === false;

// 수정 후
const fieldValue = lockedFields[allowFieldName as keyof typeof lockedFields];
// undefined나 false이면 고정 (true가 아니면 고정)
return fieldValue !== true;
```

### 3. Step2 필드들 수정

모든 Step2 필드에서 `!== false`를 `=== true`로 변경:

#### `ExclusionsPanel.tsx`
```typescript
// 수정 전
const canStudentInputExclusions = campMode
  ? lockedFields.allow_student_exclusions !== false
  : true;

// 수정 후
const canStudentInputExclusions = campMode
  ? lockedFields.allow_student_exclusions === true
  : true;
```

#### `TimeConfigPanel.tsx`
```typescript
// 수정 전
const canStudentInputTimeSettings = campMode
  ? (lockedFields.allow_student_time_settings !== false)
  : true;

// 수정 후
const canStudentInputTimeSettings = campMode
  ? (lockedFields.allow_student_time_settings === true)
  : true;
```

#### `AcademySchedulePanel.tsx`
```typescript
// 수정 전
const canStudentInputAcademySchedules = campMode
  ? (lockedFields.allow_student_academy_schedules !== false)
  : true;

// 수정 후
const canStudentInputAcademySchedules = campMode
  ? (lockedFields.allow_student_academy_schedules === true)
  : true;
```

#### `NonStudyTimeBlocksPanel.tsx`
```typescript
// 수정 전
const canStudentInputNonStudyTimeBlocks = campMode
  ? (lockedFields.allow_student_non_study_time_blocks !== false)
  : true;

// 수정 후
const canStudentInputNonStudyTimeBlocks = campMode
  ? (lockedFields.allow_student_non_study_time_blocks === true)
  : true;
```

## 🔍 동작 원리

### 수정 전 로직

```typescript
// fieldValue가 undefined일 때
undefined !== false  // true 반환 → 입력 허용 ❌
```

### 수정 후 로직

```typescript
// fieldValue가 undefined일 때
undefined === true  // false 반환 → 입력 불가 ✅

// fieldValue가 true일 때
true === true  // true 반환 → 입력 허용 ✅

// fieldValue가 false일 때
false === true  // false 반환 → 입력 불가 ✅
```

### 필드 상태별 동작

| 체크박스 상태 | fieldValue | 수정 전 | 수정 후 |
|------------|-----------|--------|--------|
| 체크함 | `true` | ✅ 입력 가능 | ✅ 입력 가능 |
| 체크 안 함 | `undefined` | ❌ 입력 가능 (문제!) | ✅ 입력 불가 |
| 명시적 해제 | `false` | ✅ 입력 불가 | ✅ 입력 불가 |

## 🧪 테스트 시나리오

### 시나리오 1: 체크박스를 체크하지 않은 경우

1. 캠프 템플릿 생성 페이지에서 "학생 입력 허용" 체크박스를 체크하지 않음
2. 템플릿 저장
3. 학생 모드에서 캠프 템플릿 작성 페이지 접속
4. 해당 필드가 비활성화되어 있는지 확인
5. 필드에 입력을 시도해도 값이 변경되지 않는지 확인

**예상 결과**:
- 필드가 비활성화되어 있음
- 입력을 시도해도 값이 변경되지 않음

### 시나리오 2: 체크박스를 체크한 경우

1. 캠프 템플릿 생성 페이지에서 "학생 입력 허용" 체크박스를 체크함
2. 템플릿 저장
3. 학생 모드에서 캠프 템플릿 작성 페이지 접속
4. 해당 필드가 활성화되어 있는지 확인
5. 필드에 입력하면 값이 변경되는지 확인

**예상 결과**:
- 필드가 활성화되어 있음
- 입력하면 값이 정상적으로 변경됨

### 시나리오 3: 모든 Step1 필드 확인

다음 필드들에 대해 위 시나리오를 반복:
- 플랜 이름
- 플랜 목적
- 스케줄러 유형
- 학습 기간
- 블록 세트
- 학습일/복습일 수
- 추가 기간 학습 범위 재배치

### 시나리오 4: 모든 Step2 필드 확인

다음 필드들에 대해 위 시나리오를 반복:
- 제외일
- 학원 일정
- 시간 설정
- 학습 시간 제외 항목

## 📝 변경된 파일 목록

1. `app/(student)/plan/new-group/_components/utils/fieldLockUtils.ts`
   - `canStudentInput` 함수: `fieldValue !== false` → `fieldValue === true`
   - `isFieldLocked` 함수: `fieldValue === false` → `fieldValue !== true`

2. `app/(student)/plan/new-group/_components/_features/scheduling/components/ExclusionsPanel.tsx`
   - `canStudentInputExclusions`: `!== false` → `=== true`

3. `app/(student)/plan/new-group/_components/_features/scheduling/components/TimeConfigPanel.tsx`
   - `canStudentInputTimeSettings`: `!== false` → `=== true`

4. `app/(student)/plan/new-group/_components/_features/scheduling/components/AcademySchedulePanel.tsx`
   - `canStudentInputAcademySchedules`: `!== false` → `=== true`

5. `app/(student)/plan/new-group/_components/_features/scheduling/components/NonStudyTimeBlocksPanel.tsx`
   - `canStudentInputNonStudyTimeBlocks`: `!== false` → `=== true`

## ✅ 검증 완료

- [x] TypeScript 타입 에러 없음
- [x] ESLint 에러 없음
- [x] 모든 Step1 필드에서 일관된 로직 적용
- [x] 모든 Step2 필드에서 일관된 로직 적용
- [x] `undefined` 상태에서 입력 불가 확인

## 🔄 관련 수정 사항

이 수정은 이전 수정들의 근본 원인을 해결합니다:
- `docs/20251222_040109-캠프-템플릿-학생-입력-제한-수정-추가.md`: `onChange` 핸들러 체크 추가
- `docs/20251222_035407-캠프-템플릿-학생-입력-제한-수정.md`: `isCampMode` prop 전달 문제 수정

## 📚 참고 사항

### 보안 고려사항

- 클라이언트 사이드 검증은 사용자 경험 개선을 위한 것
- 서버 사이드에서도 입력 허용 여부를 검증해야 함 (향후 개선 사항)
- `undefined`를 기본적으로 입력 불가로 처리하는 것이 더 안전함

### 하위 호환성

- 기존 템플릿에서 `allow_student_*` 필드가 `undefined`인 경우
- 이제는 입력 불가로 처리됨 (이전에는 입력 가능했음)
- 템플릿을 다시 저장하면 명시적으로 `true` 또는 `false`로 설정됨



