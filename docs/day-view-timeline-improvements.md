# 일별 캘린더 타임라인 개선안

## 🎯 개선 목표

1. **타임라인 중심 디자인**: 테이블 형식보다 시각적 타임라인으로 변경
2. **시간 흐름 시각화**: 시간대별로 명확하게 구분된 타임라인 표시
3. **인터랙션 개선**: 플랜 카드 클릭 시 상세 정보 표시
4. **반응형 최적화**: 모바일에서도 타임라인 형태 유지

## 📊 개선 방안

### 1. 타임라인 형식으로 변경

#### 현재 문제점
- 테이블 형식이 모바일에서 가독성 낮음
- 시간 흐름이 직관적이지 않음
- 플랜 간 관계 파악이 어려움

#### 개선안
- 왼쪽에 시간대 표시, 오른쪽에 플랜 카드 배치
- 시간대별로 세로 타임라인 라인 표시
- 각 타임슬롯을 카드 형태로 표시

### 2. 시각적 타임라인 구조

```
┌─────────────────────────────────────┐
│ 09:00 ──────────────────────────── │
│       │                            │
│ 10:00 ├─ [학습시간]                │
│       │  📚 수학 교재              │
│       │  └─ 1-10페이지             │
│       │                            │
│ 11:00 ├─ [학습시간]                │
│       │  📚 수학 교재 (이어서)     │
│       │  └─ 11-20페이지            │
│       │                            │
│ 12:00 ├─ [점심시간] 🍽️            │
│       │                            │
│ 13:00 ├─ [학원일정] 🏫             │
│       │  └─ 수학 학원              │
│       │                            │
│ 15:00 ├─ [이동시간] 🚶             │
│       │                            │
│ 16:00 ├─ [학습시간]                │
│       │  🎧 영어 강의              │
│       │  └─ 1강                    │
└─────────────────────────────────────┘
```

### 3. 타임라인 카드 디자인

- **시간대 헤더**: 왼쪽에 시간 표시, 오른쪽에 타입 배지
- **플랜 카드**: 시간대 내에 플랜들을 카드로 표시
- **연결선**: 시간대 간 세로선으로 연결감 표현

### 4. 인터랙션 개선

- 플랜 카드 호버 시 상세 정보 툴팁
- 플랜 카드 클릭 시 상세 모달 (선택사항)
- 시간대별 접기/펼치기 (선택사항)

## 🎨 구현 예시

### 타임라인 아이템 컴포넌트

```tsx
function TimelineItem({ slot, plans, academy }: TimelineItemProps) {
  return (
    <div className="relative flex gap-4">
      {/* 시간대 라인 */}
      <div className="flex flex-col items-center">
        <div className="text-sm font-semibold text-gray-700">
          {slot.start}
        </div>
        <div className="mt-1 h-full w-0.5 bg-gray-300"></div>
      </div>

      {/* 콘텐츠 영역 */}
      <div className="flex-1 pb-4">
        <div className="rounded-lg border border-gray-200 bg-white p-4">
          {/* 타입 헤더 */}
          <div className="mb-3 flex items-center justify-between">
            <div className="flex items-center gap-2">
              <span className="text-xl">{getTimeSlotIcon(slot.type)}</span>
              <span className="font-semibold text-gray-900">
                {slot.start} ~ {slot.end}
              </span>
            </div>
            <span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-medium text-blue-800">
              {slot.type}
            </span>
          </div>

          {/* 플랜 목록 */}
          {plans && plans.length > 0 && (
            <div className="flex flex-col gap-2">
              {plans.map((plan) => (
                <PlanCard key={plan.id} plan={plan} />
              ))}
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

### 전체 타임라인 뷰

```tsx
<div className="flex flex-col gap-4">
  {sortedSlots.map((slot, index) => (
    <TimelineItem
      key={index}
      slot={slot}
      plans={slot.plans}
      academy={slot.academy}
    />
  ))}
</div>
```

## 📱 반응형 고려사항

- 모바일: 시간대를 작게 표시하고 플랜 카드를 전체 너비로
- 태블릿: 시간대와 플랜 카드 비율 조정
- 데스크톱: 시간대와 플랜 카드 모두 충분한 공간 확보

## ✅ 구현 우선순위

1. **Phase 1**: 기본 타임라인 구조 구현
2. **Phase 2**: 시각적 개선 (라인, 색상)
3. **Phase 3**: 인터랙션 추가 (호버, 클릭)

