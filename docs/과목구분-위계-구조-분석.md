# 과목구분 위계 구조 분석

## 🤔 현재 구조 vs 대안

### 현재 구조 (개정교육과정의 직접 자식)

```
개정교육과정 (curriculum_revisions)
  ├─▶ 교과 (subject_groups)
  │     └─▶ 과목 (subjects)
  │           └─ subject_type_id → 과목구분
  │
  └─▶ 과목구분 (subject_types) ← 개정교육과정의 직접 자식
```

**특징:**

- `subject_types.curriculum_revision_id` → `curriculum_revisions.id`
- 과목구분이 개정교육과정에 직접 종속됨
- 교과와 같은 레벨에 위치

**장점:**

- ✅ 개정교육과정별로 과목구분이 다를 수 있음 (2022개정의 "공통"과 2015개정의 "공통"은 다를 수 있음)
- ✅ 과목구분을 개정교육과정 단위로 관리 가능
- ✅ 과목구분이 교과와 독립적 (모든 교과에서 동일한 과목구분 사용)

**단점:**

- ❓ 과목구분이 교과와 같은 레벨에 있어서 위계가 명확하지 않을 수 있음
- ❓ 실제로는 과목의 속성인데 개정교육과정의 직접 자식으로 보일 수 있음

---

### 대안 1: 과목구분을 교과의 자식으로

```
개정교육과정 (curriculum_revisions)
  └─▶ 교과 (subject_groups)
        ├─▶ 과목 (subjects)
        │     └─ subject_type_id → 과목구분
        │
        └─▶ 과목구분 (subject_types) ← 교과의 자식
```

**특징:**

- `subject_types.subject_group_id` → `subject_groups.id`
- 과목구분이 교과에 종속됨

**장점:**

- ✅ 위계가 더 명확함 (개정교육과정 → 교과 → 과목/과목구분)
- ✅ 과목구분이 교과와 더 밀접하게 연결됨

**단점:**

- ❌ 교과별로 과목구분이 다를 수 있음 (수학의 "공통"과 국어의 "공통"이 다를 수 있음)
- ❌ 실제로는 모든 교과에서 동일한 과목구분을 사용하는데 불필요한 중복 발생
- ❌ 개정교육과정별 과목구분 관리가 어려움

---

### 대안 2: 과목구분을 과목의 직접 속성으로 (현재와 유사하지만 구조 명확화)

```
개정교육과정 (curriculum_revisions)
  └─▶ 교과 (subject_groups)
        └─▶ 과목 (subjects)
              └─▶ 과목구분 (subject_types) via subject_type_id
```

**특징:**

- `subjects.subject_type_id` → `subject_types.id`
- `subject_types.curriculum_revision_id` → `curriculum_revisions.id` (개정교육과정별 관리)
- 과목구분은 개정교육과정에 종속되지만, 과목을 통해 접근

**장점:**

- ✅ 위계가 명확함: 개정교육과정 → 교과 → 과목 → 과목구분
- ✅ 개정교육과정별 과목구분 관리 가능
- ✅ 과목구분이 과목의 속성임이 명확함

**단점:**

- ❌ 현재 구조와 동일하지만, 위계 표현이 더 명확함

---

## 💡 권장 구조

**현재 구조가 맞습니다!** 다만 위계 표현을 명확히 하는 것이 좋습니다.

### 이유:

1. **과목구분은 개정교육과정에 종속적**

   - 2022개정 교육과정의 "공통"과 2015개정 교육과정의 "공통"은 의미가 다를 수 있음
   - 개정교육과정별로 과목구분 체계가 다를 수 있음

2. **과목구분은 교과와 독립적**

   - 모든 교과(국어, 수학, 영어 등)에서 동일한 과목구분(공통, 일반선택, 진로선택)을 사용
   - 교과별로 과목구분이 다르지 않음

3. **과목구분은 과목의 속성**
   - 각 과목이 하나의 과목구분을 가짐
   - 하지만 과목구분 자체는 개정교육과정에 종속됨

### 개선 제안:

위계 구조를 다음과 같이 표현하는 것이 더 명확합니다:

```
개정교육과정 (curriculum_revisions)
  │
  ├─▶ 교과 (subject_groups) ──┐
  │     └─▶ 과목 (subjects)    │
  │           └─ subject_type_id ──┘
  │                              │
  └─▶ 과목구분 (subject_types) ←─┘
        (개정교육과정별 관리, 과목에서 참조)
```

**핵심:**

- 과목구분은 개정교육과정의 직접 자식이 맞습니다
- 하지만 과목구분은 과목의 속성으로 사용됩니다
- 교과와 같은 레벨이지만, 역할이 다릅니다:
  - 교과: 개정교육과정의 내용 영역 (국어, 수학, 영어 등)
  - 과목구분: 개정교육과정의 분류 체계 (공통, 일반선택, 진로선택)

---

## 📊 실제 사용 예시

### 2022개정 교육과정

```
개정교육과정: 2022개정교육과정 (year: 2022)
  │
  ├─ 교과: 수학
  │   └─ 과목: 수학Ⅰ (subject_type_id → "공통")
  │   └─ 과목: 미적분 (subject_type_id → "일반선택")
  │
  └─ 과목구분:
      ├─ 공통
      ├─ 일반선택
      └─ 진로선택
```

### 2015개정 교육과정

```
개정교육과정: 2015개정교육과정 (year: 2015)
  │
  ├─ 교과: 수학
  │   └─ 과목: 수학 (subject_type_id → "공통")
  │   └─ 과목: 미적분 (subject_type_id → "선택")
  │
  └─ 과목구분:
      ├─ 공통
      └─ 선택 (2015개정은 "일반선택"과 "진로선택" 구분이 없을 수 있음)
```

**결론:** 개정교육과정별로 과목구분 체계가 다를 수 있으므로, 과목구분이 개정교육과정의 직접 자식인 것이 맞습니다.

---

## ✅ 최종 권장사항

**현재 구조 유지 + 위계 표현 명확화**

1. **데이터베이스 구조는 유지**

   - `subject_types.curriculum_revision_id` → `curriculum_revisions.id` (맞음)

2. **위계 표현 명확화**

   - 문서에서 위계를 다음과 같이 표현:
     ```
     개정교육과정
       ├─ 교과 (내용 영역)
       │   └─ 과목
       │       └─ 과목구분 참조
       └─ 과목구분 (분류 체계, 개정교육과정별 관리)
     ```

3. **용어 정리**
   - 교과: 개정교육과정의 내용 영역 (국어, 수학, 영어 등)
   - 과목구분: 개정교육과정의 분류 체계 (공통, 일반선택, 진로선택)
   - 둘 다 개정교육과정에 종속되지만, 역할이 다름
