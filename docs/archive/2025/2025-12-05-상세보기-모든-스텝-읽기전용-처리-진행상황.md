# 상세보기 모든 스텝 읽기 전용 처리 - 진행 상황

## 📊 현재 상태

### 완료된 작업

1. ✅ **PlanGroupDetailView 읽기 전용 모드 설정**
   - `readOnlyUpdate` 함수 생성
   - 각 Step 컴포넌트를 wrapper div로 감싸고 `pointer-events-none` 클래스 추가
   - 모든 Step에 `editable={false}` 전달

2. ✅ **Step1BasicInfo 주요 필드 수정**
   - `isDisabled` 헬퍼 함수 추가
   - `canStudentInput`에서 `!editable` 체크 추가
   - 다음 필드들의 `disabled` 조건을 `isDisabled(...)`로 변경:
     - 플랜 이름 입력 필드
     - 플랜 목적 라디오 버튼
     - 학습 기간 입력 유형 선택 버튼 (D-day, 주 단위, 직접 선택)
     - D-day 입력 필드
     - 주 단위 시작일 입력 필드
     - 주 단위 주수 조절 버튼 (-, +)
     - 직접 선택 시작일 연도 선택 필드
   - 다음 핸들러에 `if (!editable) return;` 추가:
     - 플랜 이름 `onChange`
     - 플랜 목적 `onChange`
     - `handlePeriodTypeChange` 함수
     - D-day 입력 `onChange`
     - 주 단위 입력 `onChange`
     - 주 단위 주수 조절 버튼 `onClick`
     - 직접 선택 시작일 연도 `onChange`

3. ✅ **패널 컴포넌트에 `editable` prop 추가**
   - `TimeConfigPanel`에 `editable` prop 추가
   - `NonStudyTimeBlocksPanel`에 `editable` prop 추가
   - `TimeSettingsPanel`에서 하위 패널에 `editable` 전달

### 진행 중인 작업

**Step1BasicInfo의 나머지 필드 수정 필요**:
- 직접 선택 시작일 월/일 선택 필드
- 직접 선택 종료일 연도/월/일 선택 필드
- 스케줄러 유형 선택
- 학습/복습 사이클 설정
- 블록 세트 선택 관련 필드
- 기타 입력 필드들

**예상 작업량**: 약 50개 이상의 입력 필드/버튼

### 패턴

모든 입력 필드에 적용해야 하는 패턴:

```typescript
// 1. onChange 핸들러에 editable 체크 추가
onChange={(e) => {
  if (!editable) return; // ✅ 추가
  // 기존 로직...
}}

// 2. disabled 조건을 isDisabled로 변경
disabled={isDisabled(
  // 기존 조건들...
)}
```

## 🔄 다음 단계

1. **Step1BasicInfo의 나머지 필드 수정**
   - 직접 선택 종료일 필드들
   - 스케줄러 유형 선택
   - 학습/복습 사이클 설정
   - 블록 세트 선택 관련 필드

2. **Step2TimeSettings 및 하위 패널 수정**
   - ExclusionsPanel
   - AcademySchedulePanel
   - TimeConfigPanel
   - NonStudyTimeBlocksPanel

3. **Step3ContentSelection 수정**
   - StudentContentsPanel
   - RecommendedContentsPanel

4. **Step6Simplified 수정**

## 📝 참고

- 파일 크기: Step1BasicInfo.tsx는 약 2900줄
- 입력 필드 수: 약 56개 이상의 disabled/onChange 패턴
- 작업 방식: 패턴 기반 일괄 수정 또는 섹션별 체계적 수정

---

**작업 일자**: 2025년 12월 5일
**상태**: 진행 중

