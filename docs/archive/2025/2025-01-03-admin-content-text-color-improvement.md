# 관리자 페이지 콘텐츠 관련 텍스트 색상 개선

**작성일**: 2025-01-03  
**작성자**: AI Assistant  
**점검 범위**: 관리자 페이지의 콘텐츠 관련 페이지 텍스트 색상 가이드 준수

## 📋 작업 개요

관리자 페이지의 콘텐츠 관련 페이지들에서 텍스트 색상이 너무 옅어서 가독성이 떨어지는 문제를 개선했습니다. 기존 가이드라인(`docs/camp-template-ui-text-color-improvement.md`)을 참고하여 일관된 색상 체계를 적용했습니다.

### 점검 대상 페이지

1. **콘텐츠 메타데이터 관리** (`/admin/content-metadata`)
   - 교육과정 계층 관리
   - 플랫폼 관리
   - 출판사 관리
   - 진로 계열 관리

2. **교재 관리** (`/admin/master-books`)
   - 교재 목록
   - 교재 등록
   - 교재 수정

3. **강의 관리** (`/admin/master-lectures`)
   - 강의 목록
   - 강의 등록
   - 강의 수정

---

## 🎨 개선 내용

### 색상 변경 규칙

기존 가이드라인에 따라 다음 규칙을 적용했습니다:

- **설명/안내 텍스트**: `text-gray-500` → `text-gray-700`
  - 페이지 설명
  - 폼 안내 텍스트
  - 빈 상태 메시지
  - 로딩 메시지

- **데이터 라벨**: `text-gray-500` → `text-gray-700`
  - `<dt>` 태그의 라벨
  - 카드 내 설명 텍스트

- **테이블 헤더**: `text-gray-500` → `text-gray-600`
  - 테이블 헤더는 약간 옅게 유지하되 가독성 개선

- **테이블 데이터 값**: `text-gray-500` → `text-gray-600` (순서) 또는 `text-gray-700` (데이터 값)
  - 순서 표시는 보조 정보로 `text-gray-600` 유지
  - 데이터 값은 `text-gray-700`으로 변경

---

## 📝 수정된 파일 목록

### 1. 콘텐츠 메타데이터 관리

#### 메인 페이지
- **파일**: `app/(admin)/admin/content-metadata/page.tsx`
- **변경 사항**:
  - 페이지 설명: `text-gray-500` → `text-gray-700`

#### 컴포넌트 파일들 (8개)

**PlatformsManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**PublishersManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**CareerFieldsManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**SubjectTypesManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 테이블 데이터 값: `text-gray-500` → `text-gray-700`
- 순서 표시: `text-gray-500` → `text-gray-600`

**SubjectCategoriesManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 테이블 데이터 값: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**SubjectsManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 테이블 데이터 값: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**CurriculumRevisionsManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 테이블 헤더: `text-gray-500` → `text-gray-600`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 정렬 순서 데이터: `text-gray-500` → `text-gray-600`

**CurriculumHierarchyManager.tsx**
- 로딩 메시지: `text-gray-500` → `text-gray-700`
- 빈 상태 메시지: `text-gray-500` → `text-gray-700`
- 순서 표시 텍스트: `text-gray-500` → `text-gray-600`

### 2. 교재 관리

**master-books/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`
- 페이지 설명: `text-gray-500` → `text-gray-700`
- 빈 상태 설명: `text-gray-500` → `text-gray-700`
- 카드 설명: `text-gray-500` → `text-gray-700`
- 데이터 라벨 (dt): `text-gray-500` → `text-gray-700`

**master-books/new/MasterBookForm.tsx**
- 폼 안내 텍스트 (4곳): `text-gray-500` → `text-gray-700`

**master-books/[id]/edit/MasterBookEditForm.tsx**
- 폼 안내 텍스트 (6곳): `text-gray-500` → `text-gray-700`

**master-books/new/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`

**master-books/[id]/edit/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`

### 3. 강의 관리

**master-lectures/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`
- 페이지 설명: `text-gray-500` → `text-gray-700`
- 빈 상태 설명: `text-gray-500` → `text-gray-700`
- 카드 설명: `text-gray-500` → `text-gray-700`
- 데이터 라벨 (dt): `text-gray-500` → `text-gray-700`

**master-lectures/new/MasterLectureForm.tsx**
- 폼 안내 텍스트 (5곳): `text-gray-500` → `text-gray-700`

**master-lectures/[id]/edit/MasterLectureEditForm.tsx**
- 폼 안내 텍스트 (1곳): `text-gray-500` → `text-gray-700`

**master-lectures/new/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`

**master-lectures/[id]/edit/page.tsx**
- 페이지 헤더 라벨: `text-gray-500` → `text-gray-700`

---

## ✅ 개선 효과

1. **가독성 향상**: 모든 텍스트가 더 진하게 표시되어 읽기 쉬워졌습니다.
2. **일관성 확보**: 전체적으로 일관된 색상 체계를 적용했습니다.
3. **접근성 개선**: WCAG 가이드라인에 더 부합하는 대비율을 확보했습니다.
4. **사용자 경험 개선**: 더 명확한 정보 전달로 관리자 작업 효율성 향상

---

## 🎯 적용 원칙

### 텍스트 색상 가이드라인

- **주요 텍스트**: `text-gray-900` (제목, 중요 정보)
- **일반 텍스트**: `text-gray-700` 또는 `text-gray-800` (본문, 설명)
- **보조 텍스트**: `text-gray-600` (날짜, 메타 정보, 순서)
- **테이블 헤더**: `text-gray-600` (약간 옅게 유지)

### 변경 규칙

1. **설명/안내 텍스트**: 항상 `text-gray-700` 사용
2. **데이터 라벨**: 항상 `text-gray-700` 사용
3. **테이블 헤더**: `text-gray-600` 사용 (헤더는 약간 옅게)
4. **순서/메타 정보**: `text-gray-600` 사용 (보조 정보)

---

## 📊 변경 통계

### 파일별 변경 사항

- **총 수정 파일 수**: 19개 파일
- **콘텐츠 메타데이터 관리**: 9개 파일 (1개 페이지 + 8개 컴포넌트)
- **교재 관리**: 5개 파일
- **강의 관리**: 5개 파일

### 색상별 변경 횟수

- `text-gray-500` → `text-gray-700`: 약 50+ 곳
- `text-gray-500` → `text-gray-600`: 약 30+ 곳 (테이블 헤더, 순서 표시)

---

## 📌 참고 문서

- 기존 가이드라인: `docs/camp-template-ui-text-color-improvement.md`
- 텍스트 가이드 점검 문서: `docs/2025-01-03-admin-content-text-guide-audit.md`
- 프로젝트 가이드라인: `.cursor/rules/project_rule.mdc`

---

## 🔄 향후 개선 사항

1. **알림 시스템 통일**: 일부 컴포넌트에서 `alert()` 사용 → `toast`로 변경 권장
2. **일관성 유지**: 새로 추가되는 페이지에도 동일한 색상 가이드라인 적용
3. **디자인 시스템 정립**: 텍스트 색상을 포함한 전체 디자인 시스템 문서화

---

**작업 완료 일시**: 2025-01-03
