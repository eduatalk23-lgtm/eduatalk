# 상세보기 모든 스텝 읽기 전용 처리

## 🔍 문제 상황

학생의 템플릿 상세보기에서 블록 세트 선택 문제를 해결한 후, 다른 항목들이 수정 가능한 상태로 바뀐 것 같습니다. 상세보기의 모든 스텝 항목들이 수정 불가능해야 합니다.

## 📊 원인 분석

### 현재 문제점

1. **Step 컴포넌트들이 `editable={false}`를 받지만 일부 입력 필드가 여전히 활성화됨**
   - 각 컴포넌트 내부에서 `editable` prop을 체크하지만, 모든 입력 필드에 일관되게 적용되지 않음
   - 복잡한 `disabled` 조건으로 인해 `editable={false}`일 때도 일부 필드가 활성화될 수 있음

2. **패널 컴포넌트에 `editable` prop이 누락됨**
   - `TimeConfigPanel`, `NonStudyTimeBlocksPanel`에 `editable` prop이 없음
   - 하위 패널에서 `editable` prop을 전달받지 못함

## 🛠 해결 방법

### 1. PlanGroupDetailView에서 읽기 전용 모드 강화

**파일**: `app/(student)/plan/group/[id]/_components/PlanGroupDetailView.tsx`

- `readOnlyUpdate` 함수 생성 (빈 함수)
- 각 Step 컴포넌트를 wrapper div로 감싸고 `pointer-events-none` 클래스 추가
- `editable={false}` 전달

### 2. Step1BasicInfo에 `isDisabled` 헬퍼 함수 추가

**파일**: `app/(student)/plan/new-group/_components/Step1BasicInfo.tsx`

```typescript
// editable={false}일 때 모든 필드 비활성화
const isDisabled = (additionalCondition: boolean = false) => {
  return !editable || additionalCondition;
};
```

모든 `disabled` 조건을 `isDisabled(...)`로 변경

### 3. 각 Step 컴포넌트의 입력 필드에 `editable` 체크 추가

모든 Step 컴포넌트에서:
- `disabled` 조건에 `!editable ||` 추가
- `onChange` 핸들러에서 `if (!editable) return;` 추가

### 4. 패널 컴포넌트에 `editable` prop 추가 및 전달

- `TimeConfigPanel`에 `editable` prop 추가
- `NonStudyTimeBlocksPanel`에 `editable` prop 추가
- `TimeSettingsPanel`에서 하위 패널에 `editable` 전달

## 📋 변경 사항 요약

### `app/(student)/plan/group/[id]/_components/PlanGroupDetailView.tsx`
1. `readOnlyUpdate` 함수 생성
2. 각 Step 컴포넌트를 wrapper div로 감싸고 `pointer-events-none` 클래스 추가
3. `onUpdate={readOnlyUpdate}` 설정

### `app/(student)/plan/new-group/_components/Step1BasicInfo.tsx`
1. `isDisabled` 헬퍼 함수 추가
2. `canStudentInput`에서 `!editable` 체크 추가
3. 주요 입력 필드의 `disabled` 조건을 `isDisabled(...)`로 변경
4. `onChange` 핸들러에서 `editable` 체크 추가

### `app/(student)/plan/new-group/_components/_panels/TimeConfigPanel.tsx`
1. `editable` prop 추가
2. 모든 입력 필드에 `disabled` 조건 추가 (필요 시)

### `app/(student)/plan/new-group/_components/_panels/NonStudyTimeBlocksPanel.tsx`
1. `editable` prop 추가
2. 모든 입력 필드에 `disabled` 조건 추가 (필요 시)

### `app/(student)/plan/new-group/_components/_panels/TimeSettingsPanel.tsx`
1. 하위 패널에 `editable` prop 전달

## ✅ 기대 효과

1. ✅ **완전한 읽기 전용**: 상세보기에서 모든 입력 필드가 비활성화됨
2. ✅ **일관성**: 모든 Step에서 동일한 읽기 전용 동작
3. ✅ **사용자 경험**: 수정 불가능하다는 것을 명확히 인지 가능

## 🔗 관련 파일

- `app/(student)/plan/group/[id]/_components/PlanGroupDetailView.tsx` - 읽기 전용 모드 설정
- `app/(student)/plan/new-group/_components/Step1BasicInfo.tsx` - 입력 필드 비활성화
- `app/(student)/plan/new-group/_components/_panels/TimeConfigPanel.tsx` - editable prop 추가
- `app/(student)/plan/new-group/_components/_panels/NonStudyTimeBlocksPanel.tsx` - editable prop 추가

## ⚠️ 참고

모든 입력 필드에 `disabled` 조건을 추가하는 작업은 매우 많을 수 있습니다. 따라서:
1. 주요 입력 필드부터 수정
2. `pointer-events-none` CSS 클래스로 추가 보호
3. `onUpdate` 빈 함수로 변경 방지

---

**작업 일자**: 2025년 12월 5일

