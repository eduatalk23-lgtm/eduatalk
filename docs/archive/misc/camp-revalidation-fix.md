# 캠프 모드 캐시 재검증 수정

## 🔍 문제 상황

캠프 학습 관리에서 플랜 완료 후 `/camp/today`로 리다이렉트했지만, Server Actions에서 `revalidatePath("/today")`만 호출하여 `/camp/today`가 재검증되지 않았습니다.

### 증상

터미널 로그에서 확인된 문제:

```
217: GET /camp/today?completedPlanId=...&date=2025-12-11  ✅ 정상 리다이렉트
218-221: GET /today?date=2025-12-11 (4회)                 ❌ 불필요한 요청 발생
222-223: GET /api/today/plans?camp=true                   ✅ 정상 API 호출
```

### 원인

Server Actions에서 `/today` 경로만 재검증하여 Next.js가 `/today` 페이지를 재렌더링하려고 시도하면서 불필요한 요청이 발생했습니다.

## 🛠 수정 내용

모든 Server Actions에서 `/camp/today` 경로도 함께 재검증하도록 수정했습니다.

### 수정된 파일

#### 1. `app/(student)/today/actions/todayActions.ts`

**수정된 함수들**:

- `startPlan` (96-97줄)
- `completePlan` (405-406줄)
- `postponePlan` (460-461줄)
- `endTimer` (500-501줄)
- `pausePlan` (606-607줄)
- `resumePlan` (708-709줄)
- `preparePlanCompletion` (852-853줄, 877-878줄)
- `endStudySession` (991-992줄)

**변경 패턴**:

```typescript
revalidatePath("/today");

// 변경 후
revalidatePath("/today");
revalidatePath("/camp/today");
```

#### 2. `app/(student)/today/actions/timerResetActions.ts`

- `resetPlanTimer` (112-113줄)

#### 3. `app/(student)/today/actions/planRangeActions.ts`

- `adjustPlanRanges` (78-79줄)

#### 4. `app/(student)/today/actions/planMemoActions.ts`

- `savePlanMemo` (113-114줄)

## ✅ 수정 결과

### 개선 효과

1. **캐시 동기화**: 캠프 모드와 일반 모드 간 데이터 일관성 확보
2. **불필요한 요청 제거**: `/today`로의 불필요한 재렌더링 요청 제거
3. **성능 향상**: 불필요한 네트워크 요청 감소

### 검증

- ✅ 모든 `revalidatePath("/today")` 호출에 `/camp/today` 추가 완료
- ✅ 총 12개 위치 수정 완료
- ✅ Linter 에러 없음

## 📊 수정 통계

| 파일                   | 수정된 함수 수 | 총 수정 위치 |
| ---------------------- | -------------- | ------------ |
| `todayActions.ts`      | 8개            | 9곳          |
| `timerResetActions.ts` | 1개            | 1곳          |
| `planRangeActions.ts`  | 1개            | 1곳          |
| `planMemoActions.ts`   | 1개            | 1곳          |
| **총계**               | **11개**       | **12곳**     |

## 🔗 관련 이슈

이 수정은 다음 문제와 관련이 있습니다:

- 캠프 모드에서 플랜 완료 후 일반 모드로 이동하는 문제
- 캠프 모드와 일반 모드 간 캐시 불일치 문제

---

**수정 날짜**: 2025년 1월 27일  
**상태**: ✅ 완료
