# 캠프 템플릿 생성 시 기본 블록 세트 하드코딩 영역 제거

## 작업 개요

관리자 페이지에서 캠프 템플릿 생성 시 표시되던 하드코딩된 기본 블록 세트 정보 영역을 제거했습니다.

## 작업 날짜

2024년 11월

## 변경 사항

### Step1BasicInfo.tsx - 기본 블록 세트 하드코딩 영역 제거

#### 변경 전
- 템플릿 모드에서 템플릿 ID가 없고 블록 세트가 없을 때 기본 블록 세트 정보를 하드코딩하여 표시
- "기본 블록 세트 (템플릿 저장 시 자동 생성)" 메시지와 월~일 10:00~19:00 기본값 정보 표시
- `getDefaultBlocks()` 함수를 사용하여 기본값 블록 생성

#### 변경 후
- 하드코딩된 기본 블록 세트 정보 표시 영역 제거
- 블록 세트가 실제로 선택되었을 때만 블록 세트 정보 표시
- 사용하지 않는 `getDefaultBlocks` import 제거

#### 주요 수정 내용

1. **조건부 렌더링 수정** (1970줄)
   ```typescript
   // 변경 전: 템플릿 모드에서 템플릿 ID가 없으면 표시
   {(data.block_set_id || (isTemplateMode && !templateId)) && (
   
   // 변경 후: 블록 세트가 실제로 선택되었을 때만 표시
   {data.block_set_id && (
   ```

2. **기본값 블록 표시 로직 제거** (1979-2020줄)
   - 템플릿 모드에서 기본값 블록을 표시하던 전체 로직 삭제
   - "기본 블록 세트 (템플릿 저장 시 자동 생성)" 메시지 제거
   - "기본값: 월~일 10:00~19:00" 정보 제거
   - 요일별 기본 블록 시간 표시 제거
   - "💡 블록 세트를 생성하지 않으면..." 안내 문구 제거

3. **불필요한 import 제거** (23줄)
   ```typescript
   // 제거된 import
   import { getDefaultBlocks } from "@/lib/utils/defaultBlockSet";
   ```

## 영향 범위

- **관리자 페이지**: 캠프 템플릿 생성 시 더 이상 기본 블록 세트 정보가 표시되지 않음
- **학생 페이지**: 템플릿 모드에서도 동일하게 적용됨 (블록 세트 선택 시에만 정보 표시)

## 사용자 경험 변화

### 변경 전
- 템플릿 생성 시 블록 세트를 선택하지 않아도 기본 블록 세트 정보가 표시됨
- 사용자가 기본값에 대해 알고 있다는 느낌을 받을 수 있음

### 변경 후
- 블록 세트를 실제로 선택하거나 생성해야만 블록 세트 정보가 표시됨
- 더 깔끔하고 명확한 UI
- 하드코딩된 정보 제거로 실제 데이터만 표시

## 관련 이슈

- 이전에 구현되었던 "템플릿 저장 시 기본 블록 세트 자동 생성" 로직이 실제로는 구현되지 않았음
- 하드코딩된 기본값 정보만 표시되어 사용자에게 혼란을 줄 수 있었음
- 실제 블록 세트를 생성하지 않고 템플릿을 저장하면 블록 세트가 없어 에러가 발생할 수 있음

## 다음 단계

1. 템플릿 저장 시 실제로 기본 블록 세트를 자동 생성하는 로직이 필요하다면 별도로 구현 필요
2. 현재는 블록 세트를 명시적으로 생성하거나 선택하도록 유도하는 UI로 변경됨

