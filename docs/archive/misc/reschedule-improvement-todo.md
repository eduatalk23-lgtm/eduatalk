# 재조정 기능 개선 TODO

**작성일**: 2025-12-10  
**기반 문서**: 
- [재조정 기능 시나리오](./re.md)
- [재조정 기능 구현 TODO](./reschedule-todo.md)
- [플랜 그룹 생성 저장 정보](./플랜_그룹_생성_저장_정보.md)

**목적**: 재조정 기능의 현재 구현 상태 분석 및 개선이 필요한 부분 상세 정리

---

## 📋 목차

1. [현재 구현 상태 분석](#현재-구현-상태-분석)
2. [핵심 로직 개선 사항](#핵심-로직-개선-사항)
3. [플랜 그룹 생성 옵션 반영 확인](#플랜-그룹-생성-옵션-반영-확인)
4. [UI/UX 개선 사항](#uiux-개선-사항)
5. [에지 케이스 처리 개선](#에지-케이스-처리-개선)
6. [데이터베이스 스키마 개선](#데이터베이스-스키마-개선)
7. [테스트 및 검증](#테스트-및-검증)

---

## 현재 구현 상태 분석

### ✅ 구현 완료된 부분

1. **재조정 미리보기 함수** (`app/(student)/actions/plan-groups/reschedule.ts`)
   - `_getReschedulePreview` 함수 구현 완료
   - 조정 적용 로직 (`applyAdjustments`) 사용
   - 스케줄 계산 (`calculateAvailableDates`) 호출
   - 플랜 생성 (`generatePlansFromGroup`) 호출

2. **재조정 Wizard UI** (`app/(student)/plan/group/[id]/reschedule/_components/RescheduleWizard.tsx`)
   - 3단계 Wizard 구조 구현
   - Step 1: 콘텐츠 선택
   - Step 2: 상세 조정
   - Step 3: 미리보기 & 확인

3. **조정 엔진** (`lib/reschedule/scheduleEngine.ts`)
   - `applyAdjustments` 함수 구현
   - `generateAdjustmentSummary` 함수 구현

4. **트랜잭션 처리** (`lib/reschedule/transaction.ts`)
   - `executeRescheduleTransaction` 함수 구현

### ❌ 미구현 또는 불완전한 부분

1. **재조정 실행 함수** (`app/(student)/actions/plan-groups/reschedule.ts:388-544`)
   - `_rescheduleContents` 함수에서 실제 플랜 생성 로직이 TODO로 남아있음 (489줄)
   - 새 플랜 생성 및 저장 로직 미구현

2. **미진행 범위 계산 로직**
   - 오늘 이전 플랜 조회는 하지만, 미진행 범위를 계산하여 콘텐츠 범위에 추가하는 로직 없음
   - `completed_amount` 필드를 활용한 미진행 범위 계산 미구현

3. **재조정 기간 결정 로직**
   - 오늘 이후만 재조정하도록 하는 로직 없음
   - 날짜 범위 선택 시 오늘 이후 조건 검증 없음

4. **Step 1, 2, 3 UI 컴포넌트**
   - `ContentSelectStep`, `AdjustmentStep`, `PreviewStep` 컴포넌트 구현 상태 확인 필요

---

## 핵심 로직 개선 사항

### 1. 미진행 범위 계산 및 재분배 로직 구현

**현재 상태**: 미구현

**문제점**:
- `_getReschedulePreview` 함수에서 오늘 이전 플랜을 조회하지만, 미진행 범위를 계산하여 콘텐츠 범위에 추가하는 로직이 없음
- 시나리오 4-1의 핵심 로직이 구현되지 않음

**개선 필요 사항**:

#### 1.1 미진행 범위 계산 함수 구현

**파일**: `lib/reschedule/uncompletedRangeCalculator.ts` (신규 생성)

```typescript
/**
 * 미진행 범위 계산 함수
 * 
 * 오늘 이전의 미진행 플랜 범위를 계산합니다.
 * 
 * @param plans 오늘 이전의 플랜 목록
 * @returns 콘텐츠별 미진행 범위 Map (content_id -> uncompleted_range)
 */
export function calculateUncompletedRange(
  plans: Array<{
    content_id: string;
    planned_start_page_or_time: number | null;
    planned_end_page_or_time: number | null;
    completed_amount: number | null;
  }>
): Map<string, number>
```

**TODO**:
- [ ] `lib/reschedule/uncompletedRangeCalculator.ts` 파일 생성
- [ ] `calculateUncompletedRange` 함수 구현
  - 각 플랜별 미진행 범위 계산: `(planned_end - planned_start) - (completed_amount || 0)`
  - 음수 방지: `Math.max(0, ...)`
  - 콘텐츠별 합산
- [ ] 단위 테스트 작성

#### 1.2 미진행 범위를 콘텐츠 범위에 추가하는 로직

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: `_getReschedulePreview` 함수 내부 (142줄 이후)

**TODO**:
- [ ] 오늘 날짜 구하기
- [ ] 오늘 이전 플랜만 조회 (현재는 날짜 범위 필터링만 있음)
  - `plan_date < today` 조건 추가
  - `status IN ('pending', 'in_progress')` 조건 추가
  - `is_active = true` 조건 추가
- [ ] `calculateUncompletedRange` 함수 호출
- [ ] 조정된 콘텐츠에 미진행 범위 추가
  - 각 콘텐츠의 `end_range`에 미진행 범위 추가
  - 순서: 1) 조정(adjustments) 먼저 적용, 2) 미진행 범위 추가는 나중

**참고 코드 위치**:
- `app/(student)/actions/plan-groups/reschedule.ts:121-139` (기존 플랜 조회 부분)

### 2. 재조정 기간 결정 로직 구현

**현재 상태**: 미구현

**문제점**:
- 재조정 기간이 오늘 이후만 되도록 하는 로직이 없음
- 날짜 범위 선택 시 오늘 이후 조건 검증 없음

**개선 필요 사항**:

#### 2.1 재조정 기간 결정 함수 구현

**파일**: `lib/reschedule/periodCalculator.ts` (신규 생성)

```typescript
/**
 * 재조정 기간 결정 함수
 * 
 * 오늘 이후의 재조정 기간을 결정합니다.
 * 
 * @param dateRange 사용자가 선택한 날짜 범위 (null이면 전체)
 * @param today 오늘 날짜 (YYYY-MM-DD)
 * @param groupEnd 플랜 그룹 종료일
 * @param studyDays 학습일 설정 (1730 Timetable)
 * @returns 재조정 기간 (start, end)
 */
export function getAdjustedPeriod(
  dateRange: { from: string; to: string } | null,
  today: string,
  groupEnd: string,
  studyDays?: number
): { start: string; end: string }
```

**TODO**:
- [ ] `lib/reschedule/periodCalculator.ts` 파일 생성
- [ ] `getAdjustedPeriod` 함수 구현
  - `dateRange`가 null인 경우: `adjustedPeriodStart = getNextStudyDate(today)`, `adjustedPeriodEnd = groupEnd`
  - `dateRange`가 있는 경우: `adjustedPeriodStart = max(dateRange.from, today 이후 첫 학습일)`, `adjustedPeriodEnd = dateRange.to`
  - 오늘 이후 첫 학습일 계산 함수 구현 (`getNextStudyDate`)
- [ ] 날짜 범위 검증 로직 추가
  - 선택한 범위에 오늘 이후 날짜 포함 여부 확인
  - 없으면 에러 반환
- [ ] 단위 테스트 작성

#### 2.2 재조정 기간 결정 로직 적용

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: `_getReschedulePreview` 함수 내부 (212줄 이전)

**TODO**:
- [ ] `getAdjustedPeriod` 함수 호출
- [ ] 재조정 기간을 `calculateAvailableDates`에 전달
  - `periodStart`를 `adjustedPeriodStart`로 변경
  - `periodEnd`를 `adjustedPeriodEnd`로 변경
- [ ] 생성된 플랜 필터링 (날짜 범위 선택한 경우)
  - 선택한 날짜 범위 내 플랜만 유지

**참고 코드 위치**:
- `app/(student)/actions/plan-groups/reschedule.ts:212-251` (스케줄 계산 부분)

### 3. 재조정 실행 함수 완성

**현재 상태**: 부분 구현 (TODO 주석으로 남아있음)

**문제점**:
- `_rescheduleContents` 함수에서 실제 플랜 생성 및 저장 로직이 미구현 (489줄)

**개선 필요 사항**:

#### 3.1 새 플랜 생성 및 저장 로직 구현

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: `_rescheduleContents` 함수 내부 (484-492줄)

**TODO**:
- [ ] 미리보기와 동일한 로직으로 새 플랜 생성
  - `_getReschedulePreview` 함수의 로직 재사용
  - 또는 공통 함수로 분리
- [ ] 생성된 플랜을 `student_plan` 테이블에 저장
  - 배치 insert 사용 (Supabase는 한 번에 최대 100개)
- [ ] 날짜 범위 필터링 적용 (선택한 경우)
- [ ] 저장된 플랜 수를 `plansAfterCount`에 반영

**참고 코드 위치**:
- `app/(student)/actions/plan-groups/reschedule.ts:82-369` (미리보기 함수)
- `app/(student)/actions/plan-groups/plans.ts:1729-2749` (플랜 생성 로직 참고)

#### 3.2 공통 로직 분리

**파일**: `lib/reschedule/planGenerator.ts` (신규 생성)

**TODO**:
- [ ] `generateRescheduledPlans` 함수 생성
  - 미리보기와 실행에서 공통으로 사용할 플랜 생성 로직
  - 파라미터: `groupId`, `adjustments`, `dateRange`, `today`
  - 반환: `ScheduledPlan[]`
- [ ] 미리보기와 실행 함수에서 공통 함수 호출

---

## 플랜 그룹 생성 옵션 반영 확인

### 현재 반영 상태

#### ✅ 반영되고 있는 옵션

1. **학습일/복습일 주기** (`scheduler_options.study_days`, `scheduler_options.review_days`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:197-199`
   - **상태**: ✅ 반영됨
   - `getMergedSchedulerSettings` 함수로 병합된 설정 사용
   - `calculateAvailableDates` 함수에 전달됨

2. **학원 일정** (`academySchedules`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:229-236`
   - **상태**: ✅ 반영됨
   - `getPlanGroupWithDetails` 함수로 조회
   - `calculateAvailableDates` 함수에 전달됨

3. **제외일정** (`exclusions`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:220-228`
   - **상태**: ✅ 반영됨
   - `getPlanGroupWithDetails` 함수로 조회
   - `calculateAvailableDates` 함수에 전달됨

4. **블록 세트** (`blocks`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:145-185`
   - **상태**: ✅ 반영됨
   - `getBlockSetForPlanGroup` 함수로 조회
   - `calculateAvailableDates` 함수에 전달됨

5. **시간 설정** (`scheduler_options.lunch_time`, `camp_study_hours`, 등)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:237-250`
   - **상태**: ✅ 반영됨
   - `getMergedSchedulerSettings` 함수로 병합된 설정 사용
   - `calculateAvailableDates` 함수에 전달됨

#### ⚠️ 확인이 필요한 옵션

1. **자율학습 시간 설정** (`enable_self_study_for_study_days`, `enable_self_study_for_holidays`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:241-244`
   - **상태**: ⚠️ 부분 반영
   - `group.scheduler_options`에서 직접 읽어서 사용
   - `getMergedSchedulerSettings`로 병합된 설정과 일치하는지 확인 필요

2. **지정휴일 시간 설정** (`designated_holiday_hours`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:248`
   - **상태**: ⚠️ 부분 반영
   - `group.scheduler_options`에서 직접 읽어서 사용
   - `getMergedSchedulerSettings`로 병합된 설정과 일치하는지 확인 필요

3. **비학습 시간 블록** (`non_study_time_blocks`)
   - **위치**: `app/(student)/actions/plan-groups/reschedule.ts:249`
   - **상태**: ⚠️ 부분 반영
   - `group.non_study_time_blocks`에서 직접 읽어서 사용
   - `calculateAvailableDates` 함수에서 처리되는지 확인 필요

### 개선 필요 사항

#### 1. 설정 병합 일관성 개선

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: 187-250줄 (스케줄러 설정 병합 부분)

**문제점**:
- 일부 설정은 `getMergedSchedulerSettings`로 병합하고, 일부는 `group.scheduler_options`에서 직접 읽음
- 일관성이 없어 설정이 누락되거나 불일치할 수 있음

**TODO**:
- [ ] 모든 설정을 `getMergedSchedulerSettings`로 병합하여 사용
- [ ] `getMergedSchedulerSettings` 함수가 모든 필요한 설정을 반환하는지 확인
- [ ] `non_study_time_blocks`도 병합된 설정에 포함되는지 확인
- [ ] 설정 병합 로직 테스트 작성

**참고 코드 위치**:
- `lib/data/schedulerSettings.ts` (설정 병합 로직)

#### 2. 재조정 기간 내 설정만 적용 확인

**문제점**:
- 재조정 기간이 오늘 이후만인데, 전체 기간의 설정을 사용하고 있음
- 재조정 기간 내의 제외일정, 학원일정만 필터링해야 함

**TODO**:
- [ ] 재조정 기간 결정 후 (`getAdjustedPeriod` 호출 후)
- [ ] 제외일정 필터링: 재조정 기간 내의 제외일정만 사용
- [ ] 학원일정 필터링: 재조정 기간 내의 학원일정만 사용
- [ ] 블록 세트는 전체 기간 설정 사용 (요일 기반이므로)

**참고 코드 위치**:
- `app/(student)/actions/plan-groups/reschedule.ts:220-236` (제외일정, 학원일정 전달 부분)

---

## UI/UX 개선 사항

### 1. Step 1: 콘텐츠 선택 UI

**파일**: `app/(student)/plan/group/[id]/reschedule/_components/ContentSelectStep.tsx` (확인 필요)

**TODO**:
- [ ] 파일 존재 여부 확인
- [ ] 재조정 가능 콘텐츠 목록 조회 함수 구현
  - 완료되지 않은 플랜이 있는 콘텐츠만 표시
  - 콘텐츠별 상태 표시 (available/partial/unavailable)
- [ ] 콘텐츠 카드 컴포넌트 구현
  - 콘텐츠 이름, 타입 표시
  - 현재 범위 (start_range ~ end_range) 표시
  - 진행률 표시
  - 상태 뱃지 표시
- [ ] 체크박스 선택 로직 구현
  - 다중 선택 가능
  - unavailable 상태는 비활성화
  - 전체 선택/해제 기능
- [ ] 날짜 범위 선택 UI 구현 (Range 모드)
  - DatePicker 컴포넌트 사용
  - 시작일/종료일 선택
  - 달력 UI에 플랜 존재 여부 표시
- [ ] 스마트 제안 기능 구현
  - 미진행 플랜이 많은 날짜 범위 분석
  - 추천 범위 제안 버튼
- [ ] 검증 및 에러 처리
  - 콘텐츠 미선택 검증
  - 날짜 범위 미선택 검증 (Range 모드)
  - 재조정 불가 콘텐츠 표시

**참고 코드 위치**:
- `app/(student)/plan/group/[id]/reschedule/_components/RescheduleWizard.tsx:189-199` (Step 1 렌더링 부분)

### 2. Step 2: 상세 조정 UI

**파일**: `app/(student)/plan/group/[id]/reschedule/_components/AdjustmentStep.tsx` (확인 필요)

**TODO**:
- [ ] 파일 존재 여부 확인
- [ ] 범위 조정 UI 구현
  - 콘텐츠별 범위 편집 카드
  - 시작 범위 입력 필드
  - 끝 범위 입력 필드
  - 현재 범위 표시
  - 실시간 범위 검증
- [ ] 콘텐츠 교체 UI 구현
  - "교체" 버튼
  - 콘텐츠 검색 모달
  - 교체 가능한 콘텐츠 목록 (같은 content_type)
  - 교체 후 상태 표시
- [ ] 전체 재생성 UI 구현
  - "전체 재생성" 버튼
  - 재생성 설정 모달
- [ ] 일괄 조정 UI 구현
  - 일괄 조정 모드 토글
  - 일괄 조정 패널
  - 일괄 적용 로직
- [ ] 조정 없이 진행 처리
  - adjustments 배열이 비어있어도 Step 3 진행 가능
  - "조정 없이 미진행 범위만 재분배" 안내 표시

**참고 코드 위치**:
- `app/(student)/plan/group/[id]/reschedule/_components/RescheduleWizard.tsx:200-210` (Step 2 렌더링 부분)

### 3. Step 3: 미리보기 & 확인 UI

**파일**: `app/(student)/plan/group/[id]/reschedule/_components/PreviewStep.tsx` (확인 필요)

**TODO**:
- [ ] 파일 존재 여부 확인
- [ ] 미리보기 자동 로드 구현
  - Step 3 진입 시 `getReschedulePreview` 서버 액션 호출
  - 로딩 상태 표시
  - 에러 발생 시 에러 메시지
- [ ] 변경 사항 확인 UI 구현
  - 변경 요약 카드
  - 변경 전/후 비교 테이블
  - 조정 내역 요약
  - 충돌 경고 표시
  - 일일 학습량 변화 표시
- [ ] 재조정 실행 UI 구현
  - "재조정 실행" 버튼
  - 확인 다이얼로그
  - 실행 중 상태
  - 실행 완료 처리
- [ ] 미리보기 실패 처리
  - 로딩 실패 UI
  - 조건 불만족 처리

**참고 코드 위치**:
- `app/(student)/plan/group/[id]/reschedule/_components/RescheduleWizard.tsx:211-221` (Step 3 렌더링 부분)

---

## 에지 케이스 처리 개선

### 1. 오늘 날짜 플랜 처리

**현재 상태**: 미구현

**문제점**:
- 오늘 날짜 플랜을 재조정 대상에 포함할지 제외할지 명확하지 않음

**TODO**:
- [ ] 기본 정책 결정: 오늘 날짜 제외 (권장)
  - `plan_date < today` 조건 사용
- [ ] 옵션: 오늘 날짜 포함 설정 추가 (선택 사항)
  - 설정 UI 추가
  - `plan_date <= today` 조건 변경

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: `_getReschedulePreview` 함수 내부 (121-139줄)

### 2. 모든 플랜 완료 처리

**현재 상태**: 부분 구현

**TODO**:
- [ ] Step 1에서 재조정 가능한 콘텐츠가 0개인 경우 처리
  - "재조정 가능한 콘텐츠가 없습니다." 메시지 표시
  - 재조정 페이지 진입 방지 또는 안내

**파일**: `app/(student)/plan/group/[id]/reschedule/_components/ContentSelectStep.tsx`

### 3. 남은 기간 없음 처리

**현재 상태**: 미구현

**TODO**:
- [ ] 재조정 기간 결정 후 남은 기간이 0일인 경우 검증
  - "재조정할 기간이 남아있지 않습니다." 에러 메시지
  - 재조정 진행 불가

**파일**: `lib/reschedule/periodCalculator.ts`

### 4. 날짜 범위가 오늘 이전인 경우

**현재 상태**: 미구현

**TODO**:
- [ ] 날짜 범위 검증 로직 추가
  - 선택한 범위에 오늘 이후 날짜 포함 여부 확인
  - 없으면 에러: "선택한 날짜 범위에 오늘 이후 기간이 포함되지 않았습니다."
  - 또는 자동 조정: 오늘 이후 첫 학습일로 시작일 변경

**파일**: `lib/reschedule/periodCalculator.ts`

### 5. 미진행 범위가 매우 큰 경우

**현재 상태**: 미구현

**TODO**:
- [ ] 일일 학습량 임계값 설정
  - 기준값 정의 (예: 일일 20페이지 이상)
- [ ] 경고 표시
  - "일일 학습량이 매우 큽니다. 범위 조정을 권장합니다."
  - Step 3 미리보기에서 경고 표시
- [ ] 권장 조치 안내
  - 범위 축소 권장
  - 기간 연장 권장 (플랜 그룹 설정 변경 안내)

**파일**: `app/(student)/plan/group/[id]/reschedule/_components/PreviewStep.tsx`

### 6. 동시 재조정 방지

**현재 상태**: 부분 구현 (트랜잭션 락 사용)

**TODO**:
- [ ] 트랜잭션 락 구현 확인
  - 플랜 그룹 단위 락
  - 재조정 시작 시 락 획득
  - 재조정 완료/실패 시 락 해제
- [ ] 또는 낙관적 잠금 구현
  - 버전 필드 사용
  - 충돌 시 재시도 안내

**파일**: `lib/reschedule/transaction.ts`

### 7. 변화 감지 문제 해결

**현재 상태**: 미구현

**문제점**:
- 같은 범위로 재조정했는데 변화가 없다고 표시되는 경우
- 미진행 범위를 콘텐츠 범위에 추가했지만, 플랜 생성 시 원래 범위를 사용하여 일일 학습량이 증가하지 않음

**TODO**:
- [ ] 조정된 콘텐츠 사용 보장
  - `contentsWithUncompleted` 사용
  - 원본 콘텐츠 범위 대신 조정된 범위 사용
- [ ] 변화 감지 로직 구현
  - 일일 학습량 비교
  - 플랜 개수 비교
  - 날짜 분포 비교

**파일**: `app/(student)/actions/plan-groups/reschedule.ts`

**위치**: `_getReschedulePreview` 함수 내부 (299줄 이후)

---

## 데이터베이스 스키마 개선

### 1. plan_history 테이블 확인

**현재 상태**: 사용 중 (`app/(student)/actions/plan-groups/reschedule.ts:439-464`)

**TODO**:
- [ ] 테이블 스키마 확인
  - `id`, `plan_id`, `plan_group_id`, `plan_data`, `content_id`, `adjustment_type`, `reschedule_log_id`, `created_at` 필드 확인
- [ ] 마이그레이션 파일 확인
  - 테이블 생성 마이그레이션 존재 여부 확인
  - 없으면 생성 필요

**참고 파일**:
- `supabase/migrations/` 디렉토리

### 2. reschedule_log 테이블 확인

**현재 상태**: 사용 중 (`app/(student)/actions/plan-groups/reschedule.ts:495-507`)

**TODO**:
- [ ] 테이블 스키마 확인
  - `id`, `plan_group_id`, `student_id`, `adjusted_contents`, `plans_before_count`, `plans_after_count`, `reason`, `status`, `created_at` 필드 확인
- [ ] 마이그레이션 파일 확인
  - 테이블 생성 마이그레이션 존재 여부 확인
  - 없으면 생성 필요

**참고 파일**:
- `supabase/migrations/` 디렉토리

### 3. student_plan 테이블 필드 확인

**현재 상태**: 사용 중

**TODO**:
- [ ] `completed_amount` 필드 존재 확인
- [ ] `is_active` 필드 존재 확인
- [ ] `status` 필드 값 ('pending', 'in_progress', 'completed') 확인
- [ ] 필드가 없으면 마이그레이션 생성

**참고 파일**:
- `supabase/migrations/` 디렉토리

---

## 테스트 및 검증

### 1. 단위 테스트

**TODO**:
- [ ] 미진행 범위 계산 테스트
  - 정상 케이스: 미진행 플랜 계산
  - 부분 진행 케이스: `completed_amount` 고려
  - 초과 완료 케이스: 음수 방지
- [ ] 조정 적용 테스트
  - 범위 조정 적용
  - 콘텐츠 교체 적용
  - 전체 재생성 적용
- [ ] 기간 결정 테스트
  - 전체 재조정 기간
  - 날짜 범위 지정 기간
  - 오늘 이후 시작 보장

**파일**: `lib/reschedule/__tests__/` (신규 생성)

### 2. 통합 테스트

**TODO**:
- [ ] 전체 재조정 플로우 테스트
  - Step 1 → Step 2 → Step 3 → 실행
  - 데이터 정합성 검증
- [ ] 날짜 범위 재조정 플로우 테스트
  - 범위 선택
  - 해당 범위만 재조정 확인
- [ ] 조정 + 재분배 동시 적용 테스트
  - 범위 확장 + 미진행 범위 추가

**파일**: `app/(student)/actions/plan-groups/__tests__/reschedule.test.ts` (신규 생성)

### 3. 에지 케이스 테스트

**TODO**:
- [ ] 오늘 날짜 경계 테스트
- [ ] 모든 플랜 완료 테스트
- [ ] 남은 기간 없음 테스트
- [ ] 매우 큰 미진행 범위 테스트

**파일**: `app/(student)/actions/plan-groups/__tests__/reschedule-edge-cases.test.ts` (신규 생성)

### 4. UI/UX 테스트

**TODO**:
- [ ] 반응형 디자인 테스트
- [ ] 로딩 상태 테스트
- [ ] 에러 상태 테스트
- [ ] 성공 플로우 테스트

### 5. 성능 테스트

**TODO**:
- [ ] 대량 플랜 재조정 성능
- [ ] 미리보기 응답 시간
- [ ] 실행 응답 시간

---

## 📊 진행 상황 추적

| 항목 | 상태 | 우선순위 | 예상 작업 시간 |
|------|------|----------|----------------|
| 미진행 범위 계산 로직 | ⬜ 미구현 | 높음 | 4시간 |
| 재조정 기간 결정 로직 | ⬜ 미구현 | 높음 | 3시간 |
| 재조정 실행 함수 완성 | ⬜ 미구현 | 높음 | 6시간 |
| 설정 병합 일관성 개선 | ⚠️ 부분 구현 | 중간 | 2시간 |
| Step 1 UI 구현 | ❓ 확인 필요 | 중간 | 8시간 |
| Step 2 UI 구현 | ❓ 확인 필요 | 중간 | 10시간 |
| Step 3 UI 구현 | ❓ 확인 필요 | 중간 | 8시간 |
| 에지 케이스 처리 | ⬜ 미구현 | 낮음 | 6시간 |
| 단위 테스트 | ⬜ 미구현 | 중간 | 4시간 |
| 통합 테스트 | ⬜ 미구현 | 낮음 | 6시간 |

---

## 📌 참고 사항

### 의존성 순서

1. **핵심 로직 구현** (미진행 범위 계산, 재조정 기간 결정) → 다른 모든 작업의 기반
2. **재조정 실행 함수 완성** → Step 3 UI와 연동
3. **Step 1, 2, 3 UI 구현** → 순차적으로 구현 (Step 순서)
4. **에지 케이스 처리** → 핵심 로직 완료 후
5. **테스트** → 모든 구현 완료 후

### 주요 체크포인트

- [ ] 핵심 로직 완료 후 로직 검증
- [ ] Step 3 완료 후 UI 리뷰
- [ ] 재조정 실행 함수 완료 후 통합 테스트
- [ ] 모든 작업 완료 후 최종 QA

---

**문서 버전**: 1.0  
**최종 수정일**: 2025-12-10

