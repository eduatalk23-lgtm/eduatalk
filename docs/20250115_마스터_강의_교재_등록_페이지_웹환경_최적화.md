# 마스터 강의/교재 등록 페이지 웹 환경 최적화

**작업일**: 2025-01-15  
**작업 범위**: `/admin/master-lectures/new`, `/admin/master-books/new`

## 📋 작업 개요

관리자 마스터 강의 및 교재 등록 페이지를 웹 환경에 최적화하여 더 넓은 화면을 효율적으로 활용할 수 있도록 개선했습니다.

## 🎯 주요 변경사항

### 1. 레이아웃 상수 확장

**파일**: `lib/constants/layout.ts`

- `MASTER_FORM` 타입 추가 (`max-w-5xl`)
- 마스터 콘텐츠 등록 페이지 전용 레이아웃 타입 정의
- 기존 `FORM` 타입 (`max-w-3xl`)보다 넓은 화면 활용

```typescript
/** 마스터 콘텐츠 폼 페이지 (master-lectures, master-books 등 - 웹 환경 최적화) */
MASTER_FORM: "max-w-5xl",
```

### 2. 강의 등록 페이지 (`/admin/master-lectures/new`)

**파일**: `app/(admin)/admin/master-lectures/new/page.tsx`

- 컨테이너 클래스를 `FORM` → `MASTER_FORM`으로 변경
- 페이지 너비: `max-w-3xl` → `max-w-5xl`

**파일**: `app/(admin)/admin/master-lectures/new/MasterLectureForm.tsx`

- 폼 내부 패딩: `p-6` → `p-6 md:p-8`
- 그리드 레이아웃: `md:grid-cols-2` → `md:grid-cols-2 lg:grid-cols-3`
- 큰 화면(lg 이상)에서 3열 그리드 지원
- 전체 너비 필드들: `md:col-span-2` → `md:col-span-2 lg:col-span-3`

**주요 필드 배치**:

- 강의명, URL 필드들: 3열 전체 너비
- 플랫폼: 2열 너비 (lg 화면)
- 총 회차, 총 강의시간: 각 1열
- 난이도: 1열

### 3. 교재 등록 페이지 (`/admin/master-books/new`)

**파일**: `app/(admin)/admin/master-books/new/page.tsx`

- 하드코딩된 `max-w-2xl` 제거
- `getContainerClass("MASTER_FORM", "lg")` 사용하여 일관성 확보
- 페이지 너비: `max-w-2xl` → `max-w-5xl`

**파일**: `app/(admin)/admin/master-books/new/MasterBookForm.tsx`

- 폼 내부 패딩: `p-6` → `p-6 md:p-8`
- 그리드 레이아웃: `md:grid-cols-2` → `md:grid-cols-2 lg:grid-cols-3`
- 큰 화면(lg 이상)에서 3열 그리드 지원
- 전체 너비 필드들: `md:col-span-2` → `md:col-span-2 lg:col-span-3`
- 필수 입력 항목 안내 박스: `md:col-span-2` → `md:col-span-2 lg:col-span-3`

**주요 필드 배치**:

- 교재명, 태그, URL 필드들: 3열 전체 너비
- 출판사: 1열
- 저자: 2열 너비 (lg 화면)
- 학교 유형, 최소 학년, 최대 학년: 각 1열

## 📐 반응형 레이아웃

### 화면 크기별 그리드

- **모바일 (< md)**: 1열
- **태블릿 (md ~ lg)**: 2열
- **데스크톱 (lg+)**: 3열

### 주요 개선점

1. **넓은 화면 활용**: `max-w-2xl`/`max-w-3xl` → `max-w-5xl`
2. **3열 그리드 지원**: 큰 화면에서 더 효율적인 필드 배치
3. **일관된 레이아웃**: 두 페이지 모두 동일한 컨테이너 클래스 사용
4. **향상된 패딩**: 큰 화면에서 더 여유로운 공간

## ✅ 체크리스트

- [x] 레이아웃 상수에 `MASTER_FORM` 타입 추가
- [x] 강의 등록 페이지 컨테이너 너비 확장
- [x] 교재 등록 페이지 컨테이너 너비 확장 및 일관성 확보
- [x] 폼 그리드에 3열 레이아웃 추가 (lg 화면)
- [x] 전체 너비 필드들에 `lg:col-span-3` 추가
- [x] 폼 패딩 개선 (`md:p-8` 추가)
- [x] 린터 에러 확인

## 🔍 관련 파일

- `lib/constants/layout.ts` - 레이아웃 상수
- `app/(admin)/admin/master-lectures/new/page.tsx` - 강의 등록 페이지
- `app/(admin)/admin/master-lectures/new/MasterLectureForm.tsx` - 강의 등록 폼
- `app/(admin)/admin/master-books/new/page.tsx` - 교재 등록 페이지
- `app/(admin)/admin/master-books/new/MasterBookForm.tsx` - 교재 등록 폼

## 📝 참고사항

- Spacing-First 정책 준수 (gap 사용, margin 금지)
- Tailwind CSS 유틸리티 우선 사용
- 반응형 디자인 모바일 우선 접근
- 일관된 컨테이너 클래스 사용으로 유지보수성 향상




