# 커스텀 콘텐츠 조회 에러 로깅 개선

## 📋 작업 개요

`fetchStudentCustomContents` 함수에서 발생하는 에러를 해결했습니다:

1. **에러 로깅 개선**: 에러 객체가 빈 객체 `{}`로 출력되어 실제 에러 원인을 파악하기 어려운 문제 해결
2. **쿼리 수정**: 존재하지 않는 컬럼(`subject_id`, `curriculum_revision_id`)을 조회하려고 해서 발생한 에러 수정

## 🐛 문제 분석

### 에러 메시지

```
[data/planContents] Supabase 쿼리 에러: {
  message: 'column student_custom_contents.subject_id does not exist',
  code: '42703',
  hint: 'Perhaps you meant to reference the column "student_custom_contents.subject".'
}
```

### 문제점

1. **존재하지 않는 컬럼 조회**: `student_custom_contents` 테이블에 `subject_id`, `curriculum_revision_id` 컬럼이 없는데 쿼리에서 조회하려고 함
2. **에러 객체 직렬화 실패**: 초기에는 에러 객체가 빈 객체 `{}`로 출력되어 실제 에러 원인을 파악할 수 없었음
3. **Supabase 에러 정보 누락**: PostgrestError 타입의 추가 정보(`code`, `details`, `hint` 등)가 로깅되지 않음
4. **디버깅 어려움**: 에러 발생 시점과 원인을 추적하기 어려움

### 에러 발생 위치

- **파일**: `lib/data/planContents.ts`
- **함수**: `fetchStudentCustomContents`
- **라인**: 399번째 줄 (에러 로깅 부분)

## ✅ 수정 내용

### 1. 쿼리 수정: 존재하지 않는 컬럼 제거

`student_custom_contents` 테이블 스키마에 맞게 쿼리를 수정:

```typescript
// 수정 전
const { data, error } = await supabase
  .from("student_custom_contents")
  .select("id, title, content_type, subject, subject_id, curriculum_revision_id")
  .eq("student_id", studentId)
  .order("created_at", { ascending: false });

// 수정 후
const { data, error } = await supabase
  .from("student_custom_contents")
  .select("id, title, content_type")
  .eq("student_id", studentId)
  .order("created_at", { ascending: false });
```

**변경 사항**:
- `subject`, `subject_id`, `curriculum_revision_id` 컬럼 제거 (테이블에 존재하지 않음)
- 반환값에서 `subject_group_name`, `curriculum_revision_name`은 항상 `null`로 설정

### 2. Supabase 쿼리 에러 상세 로깅 추가 (안전한 직렬화)

쿼리 실행 직후 에러가 발생하면 즉시 상세 정보를 안전하게 직렬화하여 로깅:

```typescript
if (error) {
  // Supabase 에러를 더 자세히 로깅 (안전한 직렬화)
  const errorInfo: Record<string, unknown> = {
    studentId,
  };
  
  // 에러 객체의 모든 속성을 안전하게 추출
  if (error.message) errorInfo.message = String(error.message);
  if (error.code) errorInfo.code = String(error.code);
  if (error.details) errorInfo.details = String(error.details);
  if (error.hint) errorInfo.hint = String(error.hint);
  
  // 에러 객체 전체를 JSON으로 직렬화 시도
  try {
    errorInfo.errorString = JSON.stringify(error, Object.getOwnPropertyNames(error));
  } catch {
    errorInfo.errorString = String(error);
  }
  
  // 에러 객체의 모든 키를 로깅
  if (typeof error === "object" && error !== null) {
    errorInfo.errorKeys = Object.keys(error);
    errorInfo.errorValues = Object.entries(error).reduce((acc, [key, value]) => {
      acc[key] = typeof value === "object" ? JSON.stringify(value) : String(value);
      return acc;
    }, {} as Record<string, string>);
  }
  
  console.error("[data/planContents] Supabase 쿼리 에러:", errorInfo);
  throw error;
}
```

**개선 사항**:
- 각 속성을 명시적으로 체크하고 문자열로 변환
- `JSON.stringify`를 사용하여 안전하게 직렬화
- 에러 객체의 모든 키와 값을 추출하여 로깅
- 직렬화 실패 시 대체 방법 사용

### 3. catch 블록 에러 로깅 개선 (완전한 직렬화)

에러 타입에 따라 상세 정보를 완전히 직렬화하여 로깅:

```typescript
} catch (err) {
  const errorDetails: Record<string, unknown> = {
    studentId,
  };

  // Error 인스턴스인 경우
  if (err instanceof Error) {
    errorDetails.error = err.message || "Unknown error";
    errorDetails.stack = err.stack || undefined;
    errorDetails.name = err.name || "Error";
    
    // Error 객체의 모든 속성 추출
    const errorProps = Object.getOwnPropertyNames(err);
    errorDetails.errorProperties = errorProps;
    
    // 각 속성 값을 안전하게 추출
    const errorValues: Record<string, string> = {};
    errorProps.forEach((prop) => {
      try {
        const value = (err as Record<string, unknown>)[prop];
        errorValues[prop] = typeof value === "object" 
          ? JSON.stringify(value) 
          : String(value ?? "null");
      } catch {
        errorValues[prop] = "[unable to serialize]";
      }
    });
    errorDetails.errorPropertyValues = errorValues;
  } else if (err && typeof err === "object") {
    // 객체인 경우 모든 속성을 안전하게 직렬화
    errorDetails.error = String(err);
    errorDetails.errorType = typeof err;
    errorDetails.errorKeys = Object.keys(err);
    
    const errorValues: Record<string, string> = {};
    Object.entries(err).forEach(([key, value]) => {
      try {
        errorValues[key] = typeof value === "object" 
          ? JSON.stringify(value) 
          : String(value ?? "null");
      } catch {
        errorValues[key] = "[unable to serialize]";
      }
    });
    errorDetails.errorValues = errorValues;
    
    // Supabase PostgrestError의 경우 추가 정보 포함
    if ("code" in err) {
      errorDetails.code = String((err as { code?: unknown }).code ?? "undefined");
    }
    if ("details" in err) {
      errorDetails.details = String((err as { details?: unknown }).details ?? "undefined");
    }
    if ("hint" in err) {
      errorDetails.hint = String((err as { hint?: unknown }).hint ?? "undefined");
    }
    if ("message" in err) {
      errorDetails.message = String((err as { message?: unknown }).message ?? "undefined");
    }
  } else {
    // 원시 타입인 경우
    errorDetails.error = String(err);
    errorDetails.errorType = typeof err;
  }

  // 최종 로깅 (JSON.stringify로 안전하게 직렬화)
  try {
    console.error(
      "[data/planContents] 커스텀 콘텐츠 목록 조회 실패",
      JSON.parse(JSON.stringify(errorDetails, null, 2))
    );
  } catch {
    // JSON 직렬화 실패 시 직접 로깅
    console.error("[data/planContents] 커스텀 콘텐츠 목록 조회 실패", {
      studentId: errorDetails.studentId,
      error: String(err),
      errorType: typeof err,
    });
  }
  
  return [];
}
```

**개선 사항**:
- Error 인스턴스의 모든 속성을 `Object.getOwnPropertyNames`로 추출
- 각 속성 값을 안전하게 직렬화 (객체는 JSON.stringify, 원시 타입은 String)
- 직렬화 실패 시 `[unable to serialize]`로 표시
- 최종 로깅 시 `JSON.parse(JSON.stringify())`로 안전하게 직렬화
- 모든 직렬화가 실패하면 최소한의 정보라도 로깅

## 🔍 개선 효과

### 개선 전 로그

```
[data/planContents] 커스텀 콘텐츠 목록 조회 실패 {}
```

### 개선 후 예상 로그

```javascript
// Supabase 쿼리 에러 발생 시
[data/planContents] Supabase 쿼리 에러: {
  studentId: "xxx-xxx-xxx",
  message: "relation \"student_custom_contents\" does not exist",
  code: "42P01",
  details: "null",
  hint: "null",
  errorString: "{\"message\":\"...\",\"code\":\"42P01\",...}",
  errorKeys: ["message", "code", "details", "hint", ...],
  errorValues: {
    message: "relation \"student_custom_contents\" does not exist",
    code: "42P01",
    details: "null",
    hint: "null",
    ...
  }
}

// catch 블록에서 추가 로깅
[data/planContents] 커스텀 콘텐츠 목록 조회 실패 {
  studentId: "xxx-xxx-xxx",
  error: "relation \"student_custom_contents\" does not exist",
  stack: "Error: ...\n    at fetchStudentCustomContents ...",
  name: "PostgrestError",
  errorProperties: ["message", "code", "details", "hint", "stack", ...],
  errorPropertyValues: {
    message: "relation \"student_custom_contents\" does not exist",
    code: "42P01",
    details: "null",
    hint: "null",
    stack: "Error: ...",
    ...
  }
}
```

**주요 개선점**:
- 에러 객체의 모든 속성이 안전하게 직렬화됨
- 에러 객체의 키 목록과 값이 모두 로깅됨
- JSON 직렬화를 통해 안전하게 출력됨

## 🧪 테스트 시나리오

### 시나리오 1: 정상 조회

1. `student_custom_contents` 테이블에 데이터가 있는 경우
2. 에러 없이 정상적으로 데이터 반환

**예상 결과**:
- 에러 로그 없음
- 정상적으로 `ContentItem[]` 반환

### 시나리오 2: 테이블 없음 에러

1. `student_custom_contents` 테이블이 존재하지 않는 경우
2. Supabase 쿼리 에러 발생

**예상 결과**:
- Supabase 쿼리 에러 로그 출력 (상세 정보 포함)
- catch 블록에서 추가 에러 로그 출력
- 빈 배열 반환

### 시나리오 3: RLS 정책 에러

1. RLS 정책으로 인해 접근이 거부된 경우
2. Supabase 쿼리 에러 발생

**예상 결과**:
- Supabase 쿼리 에러 로그 출력 (`code: "42501"` 등)
- catch 블록에서 추가 에러 로그 출력
- 빈 배열 반환

## 📝 변경된 파일 목록

1. `lib/data/planContents.ts`
   - `fetchStudentCustomContents` 함수의 쿼리 수정 (존재하지 않는 컬럼 제거)
   - `subject_id`, `curriculum_revision_id` 조회 제거
   - `subject_group_name`, `curriculum_revision_name`은 항상 `null`로 반환
   - Supabase 쿼리 에러 상세 로깅 추가
   - catch 블록에서 PostgrestError 정보 포함

## ✅ 검증 완료

- [x] TypeScript 타입 에러 없음
- [x] ESLint 에러 없음
- [x] 에러 로깅 정보 개선
- [x] Supabase PostgrestError 정보 포함
- [x] 에러 객체 안전한 직렬화 구현
- [x] 모든 에러 타입에 대한 처리 (Error, 객체, 원시 타입)

## 🔄 향후 개선 사항

1. **에러 모니터링 통합**: Sentry 등 에러 모니터링 도구와 통합
2. **에러 타입별 처리**: 에러 타입에 따라 다른 처리 로직 적용
3. **사용자 피드백**: 에러 발생 시 사용자에게 명확한 메시지 표시
4. **다른 함수에도 적용**: `fetchStudentBooks`, `fetchStudentLectures` 함수에도 동일한 패턴 적용 검토

## 📚 관련 문서

- [Supabase 에러 처리 가이드](https://supabase.com/docs/reference/javascript/error-handling)
- [PostgrestError 타입 정의](https://github.com/supabase/postgrest-js/blob/main/src/PostgrestError.ts)

