# 🔍 Wizard Phase 4 분석 및 설계

**작성일**: 2025년 11월 29일  
**Phase**: 4.1 - 분석 및 설계  
**대상**: Step6FinalReview 간소화

---

## 📋 현재 상황 분석

### Step6FinalReview.tsx

**파일 크기**: 2,625 라인  
**복잡도**: 매우 높음  
**문제점**:
- 모든 정보를 한 화면에 표시 (정보 과부하)
- 범위 수정 기능 포함 (Step 3에서 하는 것이 더 직관적)
- 읽기/편집 모드가 혼재
- 3개 이상의 큰 섹션이 항상 펼쳐져 있음

---

## 🎯 Phase 4 목표

### 간소화 방향

1. **읽기 전용 중심**
   - 확인용 정보만 표시
   - 수정은 "해당 단계로 돌아가기" 버튼

2. **섹션별 접기/펼치기**
   - 기본: 모두 접힘
   - 클릭 시 펼침
   - 중요 정보만 요약 표시

3. **명확한 수정 프로세스**
   - "Step X로 돌아가기" 버튼
   - 현재 단계에서 직접 수정 불가

---

## 📊 Step6 구조 분석

### 주요 섹션 (6개)

#### 1. 전체 학습량 비교
- **현재**: 항상 표시
- **개선**: 요약 카드 (접기/펼치기)
- **정보**: 현재 범위 / 추천 범위 / 차이

#### 2. 학생 콘텐츠 목록
- **현재**: 전체 목록 + 범위 수정 UI
- **개선**: 개수 + 과목별 요약
- **수정**: "Step 3로 돌아가기" 버튼

#### 3. 추천 콘텐츠 목록
- **현재**: 전체 목록 + 범위 수정 UI
- **개선**: 개수 + 과목별 요약
- **수정**: "Step 3로 돌아가기" 버튼

#### 4. 과목별 학습 범위
- **현재**: 과목별 상세 정보
- **개선**: 과목별 요약 (총 페이지/강 수)
- **수정**: "Step 3로 돌아가기" 버튼

#### 5. 전략과목/취약과목 정보
- **현재**: 과목별 상세 설정
- **개선**: 과목별 요약 (전략/취약 표시)
- **수정**: 읽기 전용 (캠프 모드에서만 표시)

#### 6. 기본 정보 (숨김)
- **정보**: 이름, 기간, 스케줄러 등
- **위치**: 별도 섹션으로 추가
- **수정**: "Step 1로 돌아가기" 버튼

---

## 🎨 새로운 UI 구조

### 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│ 최종 확인                                                │
│ 플랜을 생성하기 전 마지막으로 확인해주세요              │
├─────────────────────────────────────────────────────────┤
│                                                           │
│ ▶ 기본 정보                                 [Step 1로]  │
│   2024-11-01 ~ 2024-12-31 (60일)                        │
│                                                           │
│ ▶ 시간 설정                                 [Step 2로]  │
│   학원 2개, 제외일 5일                                  │
│                                                           │
│ ▼ 콘텐츠 선택                               [Step 3로]  │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 총 9개 (학생 5개 + 추천 4개)                        │ │
│ │                                                       │ │
│ │ 📚 학생 콘텐츠 (5개)                                  │ │
│ │ • 국어: 2개 (500페이지)                              │ │
│ │ • 수학: 2개 (400페이지)                              │ │
│ │ • 영어: 1개 (300페이지)                              │ │
│ │                                                       │ │
│ │ ⭐ 추천 콘텐츠 (4개)                                  │ │
│ │ • 과학: 2개 (200페이지)                              │ │
│ │ • 사회: 2개 (150페이지)                              │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                           │
│ ▶ 학습량 비교                                            │
│   현재: 1,550페이지 / 추천: 1,200~1,800페이지          │
│                                                           │
│ ▶ 전략과목/취약과목 (캠프 모드만)                        │
│   전략: 수학(3일), 취약: 영어                           │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🧩 컴포넌트 구조

### 공통 컴포넌트 (3개)

#### 1. CollapsibleSection
```typescript
type CollapsibleSectionProps = {
  title: string;
  defaultOpen?: boolean;
  onEdit?: () => void;
  editLabel?: string;
  children: React.ReactNode;
};
```

**기능**:
- 접기/펼치기
- 수정 버튼 (옵션)
- 아이콘 애니메이션

#### 2. SummaryCard
```typescript
type SummaryCardProps = {
  title: string;
  value: string | number;
  subtitle?: string;
  icon?: React.ReactNode;
  variant?: "default" | "primary" | "success" | "warning";
};
```

**기능**:
- 요약 정보 표시
- 아이콘 지원
- 색상 변형

#### 3. SectionSummary
```typescript
type SectionSummaryProps = {
  items: Array<{
    label: string;
    value: string | number;
  }>;
};
```

**기능**:
- 리스트 형태 요약
- 키-값 쌍 표시

---

### 섹션별 컴포넌트 (6개)

#### 1. BasicInfoSummary
- 플랜 이름
- 학습 기간
- 목적
- 스케줄러 타입

#### 2. TimeSettingsSummary
- 학원 일정 개수
- 제외일 개수
- 시간 설정 요약

#### 3. ContentsSummary
- 총 콘텐츠 개수
- 학생/추천 구분
- 과목별 요약
- 총 페이지/강 수

#### 4. LearningVolumeSummary
- 현재 학습량
- 추천 범위
- 차이 (초과/부족)

#### 5. SubjectAllocationSummary (캠프 모드만)
- 전략과목 목록
- 취약과목 목록
- 과목별 배정일

#### 6. ScheduleSummary (선택)
- 일별 학습 시간
- 주별 학습 일수

---

## 🔄 데이터 흐름

### 기존 (Step6FinalReview)
```
WizardData
  → Step6FinalReview
    → 모든 섹션 렌더링 (2,625 라인)
    → 범위 수정 UI
    → 상세 정보 API 호출
```

### 개선 (Step6Simplified)
```
WizardData
  → Step6Simplified
    ├→ CollapsibleSection (기본 정보)
    │   └→ BasicInfoSummary (요약)
    ├→ CollapsibleSection (시간 설정)
    │   └→ TimeSettingsSummary (요약)
    ├→ CollapsibleSection (콘텐츠) [기본 펼침]
    │   └→ ContentsSummary (요약)
    ├→ CollapsibleSection (학습량)
    │   └→ LearningVolumeSummary (요약)
    └→ CollapsibleSection (전략/취약) [캠프 모드만]
        └→ SubjectAllocationSummary (요약)
```

---

## 📐 상세 설계

### CollapsibleSection

**기능**:
- 기본 접힘/펼침 상태
- 클릭으로 토글
- 화살표 아이콘 회전
- 수정 버튼 (옵션)

**상태**:
- `isOpen`: boolean

**애니메이션**:
- height: auto → 0 (collapse)
- arrow: rotate(0deg) → rotate(90deg)

---

### ContentsSummary

**표시 정보**:
1. **총 개수**: "총 9개 (학생 5개 + 추천 4개)"
2. **과목별 요약**:
   ```
   📚 학생 콘텐츠 (5개)
   • 국어: 2개 (교재 1, 강의 1) - 500페이지
   • 수학: 2개 (교재 2) - 400페이지
   • 영어: 1개 (강의 1) - 30강
   
   ⭐ 추천 콘텐츠 (4개)
   • 과학: 2개 - 200페이지
   • 사회: 2개 - 150페이지
   ```

3. **필수 과목 체크**:
   - ✅ 국어: 2개
   - ✅ 수학: 2개
   - ✅ 영어: 1개

**계산**:
- 과목별 그룹핑
- 타입별 카운트 (교재/강의)
- 총 페이지/강 수 합계

---

### LearningVolumeSummary

**표시 정보**:
1. **현재 학습량**: 1,550페이지
2. **추천 범위**: 1,200~1,800페이지
3. **상태**:
   - ✅ 적정 (범위 내)
   - ⚠️ 부족 (범위 미달)
   - ⚠️ 초과 (범위 초과)

**계산**:
```typescript
const currentVolume = contents.reduce((sum, c) => 
  sum + (c.end_range - c.start_range + 1), 0
);

const recommendedMin = Math.floor(currentVolume * 0.8);
const recommendedMax = Math.ceil(currentVolume * 1.2);

const status = 
  currentVolume < recommendedMin ? "부족" :
  currentVolume > recommendedMax ? "초과" :
  "적정";
```

---

## 🎯 구현 우선순위

### P0 (필수)
1. CollapsibleSection
2. ContentsSummary
3. BasicInfoSummary
4. TimeSettingsSummary

### P1 (중요)
5. LearningVolumeSummary
6. SummaryCard

### P2 (선택)
7. SubjectAllocationSummary
8. ScheduleSummary

---

## 📊 예상 효과

### 코드 감소
- 기존: 2,625 라인
- 목표: 800 라인
- 감소: **70%** (1,825 라인)

### 컴포넌트 분리
- 기존: 1개 (Step6FinalReview)
- 개선: 9개 (메인 1 + 공통 3 + 섹션 5)
- 증가: **800%**

### 사용자 경험
- ✅ 정보 과부하 방지 (접기/펼치기)
- ✅ 명확한 수정 프로세스 (단계 이동)
- ✅ 빠른 로딩 (불필요한 API 호출 제거)
- ✅ 모바일 친화적 (적은 정보량)

---

## 🔧 기술적 고려사항

### 성능
- 접힌 섹션은 렌더링하지 않음 (lazy)
- 필요한 정보만 계산
- API 호출 최소화

### 접근성
- 키보드 네비게이션
- ARIA 속성 (expanded, controls)
- 스크린 리더 지원

### 애니메이션
- CSS transition
- smooth collapse/expand
- 60fps 유지

---

## 📝 다음 단계

### Phase 4.2: 공통 컴포넌트 (3시간)
1. CollapsibleSection.tsx
2. SummaryCard.tsx
3. SectionSummary.tsx

### Phase 4.3: 섹션별 컴포넌트 (6시간)
1. BasicInfoSummary.tsx
2. TimeSettingsSummary.tsx
3. ContentsSummary.tsx
4. LearningVolumeSummary.tsx
5. SubjectAllocationSummary.tsx

### Phase 4.4: Step6 재구성 (2시간)
- Step6Simplified.tsx 생성
- Wizard 통합

---

**작성일**: 2025년 11월 29일  
**소요 시간**: 2시간  
**상태**: Phase 4.1 완료  
**다음**: Phase 4.2 공통 컴포넌트 구현

