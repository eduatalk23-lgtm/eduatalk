# 1730 Timetable 구현 가이드

> 1730 Timetable 기능의 구현, 점검, 필수 입력 항목, 단계별 로직을 통합한 종합 가이드 문서입니다.

**작성일**: 2025-01-31  
**버전**: 1.0

---

## 📋 목차

1. [구현 점검 리포트](#구현-점검-리포트)
2. [필수 입력 항목](#필수-입력-항목)
3. [단계별 입력 항목 및 로직 연결](#단계별-입력-항목-및-로직-연결)
4. [개선 사항](#개선-사항)

---

## 구현 점검 리포트

### ✅ 잘 구현된 부분

#### Step 1: 기본 정보
- ✅ 플랜 이름 (`name`)
- ✅ 플랜 목적 (`plan_purpose`)
- ✅ 스케줄러 유형 (`scheduler_type`)
- ✅ 기간 설정 (`period_start`, `period_end`)
- ✅ 목표 날짜 (`target_date`) - 선택사항
- ✅ 블록 세트 선택 (`block_set_id`)
- ✅ 1730 Timetable 옵션:
  - ✅ 학습일 수 (`study_days`) - 슬라이더 (1-7)
  - ✅ 복습일 수 (`review_days`) - 슬라이더 (0-3)
  - ✅ 복습 범위 (`review_scope`) - 드롭다운 ("full" | "partial")

#### Step 2: 블록 및 제외일
- ✅ 학습 제외일 (`exclusions`)
- ✅ 학원 일정 (`academy_schedules`)
- ✅ 시간 설정 (`time_settings`)
- ✅ 학습 시간 제외 항목 (`non_study_time_blocks`)

#### Step 2.5: 스케줄 확인
- ✅ 스케줄 가용성 계산
- ✅ 요약 정보 표시
- ✅ 일별 스케줄 상세 정보 표시

#### Step 3: 콘텐츠 선택
- ✅ 학생 콘텐츠 선택 (`student_contents`)
- ✅ 콘텐츠 범위 설정 (`start_range`, `end_range`)
- ✅ 상세 단위 선택 (페이지/회차)
- ✅ 과목 카테고리 자동 수집 (`subject_category`)

#### Step 4: 추천 콘텐츠
- ✅ 추천 콘텐츠 조회 및 표시
- ✅ 추천 이유 표시
- ✅ 추천 콘텐츠 선택 및 추가
- ✅ 필수 과목 현황 표시

#### Step 6: 최종 확인
- ✅ 학습일/복습일 주기 조정 (`study_review_cycle`)
- ✅ 교과 제약 조건 (`subject_constraints`)
- ✅ 추가 기간 재배치 (`additional_period_reallocation`)
- ✅ 콘텐츠 범위 수정 기능

#### Step 7: 스케줄 결과
- ✅ 생성된 플랜 표시
- ✅ 플랜 미리보기 및 재생성 기능

### ❌ 누락된 필수 입력 항목

#### 1. 전략과목/취약과목 정보 (필수)

**현재 상태**: ❌ **완전히 누락**

**요구사항**:
```typescript
{
  subject_id: string;
  subject_name: string;
  subject_type: "strategy" | "weakness";
  weekly_days?: number; // 전략과목인 경우: 2, 3, 4
}
```

**영향**:
- 소요시간 계산 시 과목별 보정 계수가 적용되지 않음
  - 취약과목: ×1.2
  - 전략과목: ×1.0~1.1
- 과목 배정 방식 결정 불가

**권장 해결 방법**:
1. Step 3 또는 Step 4에서 각 콘텐츠 선택 시 과목 유형 선택 UI 추가
2. 또는 Step 6에서 과목별 유형 설정 섹션 추가
3. `WizardData` 타입에 `subject_allocations` 필드 추가

#### 2. 학생 수준 정보 (필수)

**현재 상태**: ❌ **완전히 누락**

**요구사항**:
```typescript
{
  student_level: "high" | "medium" | "low";
}
```

**영향**:
- 소요시간 계산 시 학생 수준 보정 계수가 적용되지 않음
  - 상위 수준 (high): 0.8~0.9
  - 중위 수준 (medium): 1.0
  - 하위 수준 (low): 1.1~1.3

**권장 해결 방법**:
1. Step 1에 학생 수준 선택 UI 추가
2. 또는 학생 프로필에서 가져오기
3. `WizardData` 타입에 `student_level` 필드 추가

### ⚠️ 개선이 필요한 부분

#### 1. 데이터 흐름 일관성
- `study_review_cycle`과 `scheduler_options`에 중복 저장
- 단일 소스로 통일 필요

#### 2. 필수 과목 검증
- `Step6FinalReview.tsx`에서 필수 과목 경고 표시는 있음
- 플랜 생성 전 필수 과목 검증 로직 강화 필요

#### 3. 추천 범위 계산 로직
- 계산 실패 시 사용자에게 명확한 안내 필요
- 계산 로직 문서화 필요

### 📊 구현 완성도 요약

| 항목                       | 상태 | 완성도                         |
| -------------------------- | ---- | ------------------------------ |
| Step 1: 기본 정보          | ✅   | 100%                           |
| Step 2: 블록 및 제외일     | ✅   | 100%                           |
| Step 2.5: 스케줄 확인      | ✅   | 100%                           |
| Step 3: 콘텐츠 선택        | ✅   | 100%                           |
| Step 4: 추천 콘텐츠        | ✅   | 100%                           |
| Step 6: 최종 확인          | ⚠️   | 90% (필수 과목 검증 보완 필요) |
| Step 7: 스케줄 결과        | ✅   | 100%                           |
| **전략과목/취약과목 정보** | ❌   | **0% (누락)**                  |
| **학생 수준 정보**         | ❌   | **0% (누락)**                  |

**전체 완성도**: 약 **85%**

---

## 필수 입력 항목

### 1. 기본 정보

#### 필수 입력 항목
- **플랜 이름**: string
- **플랜 목적**: `"내신대비" | "모의고사" | "수능" | "기타"`
- **스케줄러 유형**: `"자동스케줄러" | "1730_timetable"`

#### 선택 입력 항목
- **목표 날짜**: `YYYY-MM-DD` (D-day)
- **블록 세트 ID**: UUID

### 2. 기간 및 시간 설정

#### 기간 정보 (필수)
- **period_start**: `YYYY-MM-DD`
- **period_end**: `YYYY-MM-DD`
- 제약조건: `period_end > period_start`

#### 학습 시간 설정 (필수)
- **start_time**: `HH:mm` (24시간 형식)
- **end_time**: `HH:mm` (24시간 형식)
- 제약조건: `start_time < end_time`

#### 자율학습 시간 설정 (선택)
- **enabled**: boolean
- **start_time**: `HH:mm`
- **end_time**: `HH:mm`
- **allow_on_holiday**: boolean
- **enable_self_study_for_study_days**: boolean

⚠️ **중요 제약사항**: 자율학습 시간에는 기본적으로 플랜 배정을 하지 않음. 배정 가능한 시간이 부족한 경우에만 사용.

#### 학습 시간 제외 항목 설정 (선택)
- **type**: `"아침식사" | "점심식사" | "저녁식사" | "수면" | "기타"`
- **start_time**: `HH:mm`
- **end_time**: `HH:mm`
- **days_of_week**: `0-6` 배열 (없으면 매일)
- **description**: string

### 3. 학습 제외 설정

#### 학습 제외일 설정 (선택)
- **exclusion_date**: `YYYY-MM-DD`
- **exclusion_type**: `"휴가" | "개인일정" | "기타" | "지정휴일"`
- **reason**: string

#### 학원 일정 설정 (선택)
- **day_of_week**: `0-6` (0: 일요일, 6: 토요일)
- **start_time**: `HH:mm`
- **end_time**: `HH:mm`
- **travel_time**: number (분 단위)
- **academy_name**: string
- **subject**: string

### 4. 학습일/복습일 주기 설정 (1730 Timetable 필수)

- **study_days**: number (기본값: 6)
- **review_days**: number (기본값: 1)

**제약조건**:
- `study_days >= 1`
- `review_days >= 0`
- 주기 길이 = `study_days + review_days` (최소 1 이상)

⚠️ **중요**: 학습 제외일은 주기 계산에서 제외됨. 실제 날짜 기준이 아닌, 학습일과 복습일만 합쳐서 주기 길이 계산.

### 5. 콘텐츠 정보 (필수)

- **content_type**: `"book" | "lecture" | "custom"`
- **content_id**: UUID
- **start_range**: number (학습 시작 범위)
- **end_range**: number (학습 종료 범위)

### 6. 전략과목/취약과목 정보 (1730 Timetable 필수) ⚠️ 누락

- **subject_id**: UUID/텍스트
- **subject_name**: string
- **subject_type**: `"strategy" | "weakness"`
- **weekly_days**: number (전략과목인 경우: 2, 3, 4)

**배정 방식**:
- **취약과목 (weakness)**: 전체 학습일에 플랜 배정
- **전략과목 (strategy)**: 주당 배정 일수에 따라 배정

### 7. 학생 수준 정보 (1730 Timetable 필수) ⚠️ 누락

- **student_level**: `"high" | "medium" | "low"`

**학생 수준별 보정 계수**:
- 상위 수준 (high): 0.8~0.9
- 중위 수준 (medium): 1.0
- 하위 수준 (low): 1.1~1.3

### 8. 교과 제약 조건 (선택)

- **required_subjects**: string[] (필수 교과 목록)
- **excluded_subjects**: string[] (제외 교과 목록)
- **constraint_handling**: `"strict" | "warning" | "auto_fix"`

### 9. 추가 기간 재배치 (선택)

- **period_start**: `YYYY-MM-DD`
- **period_end**: `YYYY-MM-DD`
- **type**: `"additional_review"`
- **subjects**: string[] (재배치할 과목 목록)
- **review_of_review_factor**: number (0.0~1.0)

---

## 단계별 입력 항목 및 로직 연결

### Step 1: 기본 정보

#### 입력 항목
- `name`, `plan_purpose`, `scheduler_type`, `period_start`, `period_end`, `target_date`, `block_set_id`
- `scheduler_options.study_days`, `scheduler_options.review_days`

#### 로직 연결
```
period_start, period_end
  → calculateStudyReviewCycle()
  → CycleDayInfo[] (학습일/복습일 분류)

scheduler_type = "1730_timetable"
  → 1730 Timetable 로직 활성화
  → study_days, review_days 사용

block_set_id
  → 블록 세트 조회
  → BlockInfo[] (요일별 시간대 정보)
```

#### 저장 위치
- `plan_groups` 테이블: `name`, `plan_purpose`, `scheduler_type`, `period_start`, `period_end`, `target_date`, `block_set_id`
- `plan_groups.scheduler_options` (JSONB): `study_days`, `review_days`

### Step 2: 블록 및 제외일

#### 입력 항목
- `exclusions[]`, `academy_schedules[]`, `time_settings`, `non_study_time_blocks[]`

#### 로직 연결
```
period_start, period_end, exclusions
  → calculateStudyReviewCycle()
  → CycleDayInfo[] (제외일 제외한 주기 계산)

exclusions, academy_schedules, non_study_time_blocks
  → calculateAvailableDates()
  → ScheduleAvailabilityResult (날짜별 학습 가능 시간)

time_settings.camp_study_hours
  → 학습 시간대 설정
  → study_hours (JSONB)

time_settings.camp_self_study_hours
  → 자율학습 시간대 설정
  → self_study_hours (JSONB)
```

#### 저장 위치
- `plan_groups` 테이블: `study_hours`, `self_study_hours`, `non_study_time_blocks` (JSONB)
- `plan_exclusions` 테이블: 제외일 정보
- `academy_schedules` 테이블: 학원 일정 정보

### Step 2.5: 스케줄 확인

이 단계는 입력 단계가 아니라 **Step 2의 결과를 확인**하는 단계입니다.

#### 로직 연결
```
Step 2 입력 데이터
  → calculateAvailableDates()
  → ScheduleAvailabilityResult
    ├─ summary: 통계 정보
    ├─ daily_schedule: 일별 스케줄 정보
    └─ errors: 오류 메시지
```

#### 출력 결과
- **schedule_summary**: 총 학습일 수, 총 복습일 수, 총 학습 시간 등
- **daily_schedule**: 날짜별 학습 가능 시간, 제외 시간, 타임라인 등

#### 저장 위치
- `plan_groups.daily_schedule` (JSONB): 일별 스케줄 정보

### Step 3: 콘텐츠 선택

#### 입력 항목
- `student_contents[]` (content_type, content_id, start_range, end_range, subject_category)

#### 로직 연결
```
student_contents + schedule_summary
  → 학습 범위 추천 로직
  → 적정 학습 범위 제안

student_contents[].subject_category
  → 교과 제약 조건 검증
  → validateSubjectConstraints()
```

#### 저장 위치
- `plan_contents` 테이블: 콘텐츠 정보

### Step 4: 추천 콘텐츠

#### 입력 항목
- `recommended_contents[]` (content_type, content_id, start_range, end_range, subject_category)

#### 로직 연결
```
recommended_contents
  → student_contents와 병합
  → 전체 콘텐츠 목록 생성
```

#### 저장 위치
- `plan_contents` 테이블: 콘텐츠 정보

### Step 6: 최종 확인

#### 입력 항목
- `study_review_cycle` (학습일/복습일 주기 조정)
- `subject_constraints` (교과 제약 조건)
- `additional_period_reallocation` (추가 기간 재배치)

#### 로직 연결
```
study_review_cycle
  → 주기 재계산
  → CycleDayInfo[] 업데이트

subject_constraints
  → validateSubjectConstraints()
  → 제약 조건 검증

additional_period_reallocation
  → 추가 기간 플랜 재배치
  → 복습의 복습 로직 적용
```

#### 저장 위치
- `plan_groups.subject_constraints` (JSONB)
- `plan_groups.additional_period_reallocation` (JSONB)
- `plan_groups.scheduler_options` (JSONB): `study_review_cycle` 병합

### Step 7: 스케줄 결과

#### 로직 연결
```
전체 입력 데이터
  → generatePlansFromGroupAction()
  → 플랜 생성
  → student_plan 테이블에 저장
```

---

## 개선 사항

### 우선순위 1: 필수 입력 항목 추가

1. **전략과목/취약과목 정보 입력 UI 추가**
   - Step 3 또는 Step 6에 과목별 유형 설정 섹션 추가
   - 각 과목에 대해 "전략과목" 또는 "취약과목" 선택
   - 전략과목인 경우 주당 배정 일수 선택 (2, 3, 4)

2. **학생 수준 정보 입력 UI 추가**
   - Step 1에 학생 수준 선택 드롭다운 추가
   - 또는 학생 프로필에서 자동으로 가져오기

### 우선순위 2: 데이터 일관성 개선

1. **`study_review_cycle`과 `scheduler_options` 통일**
   - 단일 소스로 통일하여 중복 제거

2. **필수 과목 검증 강화**
   - Step 6에서 필수 과목이 없으면 다음 단계로 진행 불가
   - 검증 오류 메시지 명확화

### 우선순위 3: 문서화 개선

1. **데이터 변환 로직 문서화**
   - `time_settings` → `scheduler_options` 병합 로직
   - `plan_purpose` 정규화/비정규화 로직

2. **추천 범위 계산 로직 문서화**
   - 계산 공식 및 제약사항 명시

---

## 📚 참고 문서

- [플랜-생성-로직-설계.md](../../timetable/플랜-생성-로직-설계.md)
- [학습일-복습일-주기-설정.md](../../timetable/학습일-복습일-주기-설정.md)
- [1730Timetable-솔루션-요구사항-및-플랜-예시.md](../../timetable/1730Timetable-솔루션-요구사항-및-플랜-예시.md)
- [요구사항-정리.md](../../timetable/요구사항-정리.md)

---

**작성일**: 2025-01-31  
**작성자**: AI Assistant  
**버전**: 1.0

