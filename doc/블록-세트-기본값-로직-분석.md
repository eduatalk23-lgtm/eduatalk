# 블록 세트 기본값 설정 및 반영 로직 분석

## 📋 현재 구현 상태

### 1. 템플릿 생성 시 블록 세트 기본값 로직

#### 위치: `app/(student)/plan/new-group/_components/Step1BasicInfo.tsx`

**현재 동작**:
- 템플릿 모드에서 블록 세트를 생성할 때, `addedBlocks`가 비어있으면 기본값 블록을 자동 생성합니다.
- 기본값: 월~일(1~0) 10:00~19:00

```typescript:417:426:app/(student)/plan/new-group/_components/Step1BasicInfo.tsx
// 2. 추가된 블록들을 실제로 추가
// 추가된 블록이 없으면 기본값(월~일 10:00~19:00) 생성
const blocksToAdd =
  addedBlocks.length > 0
    ? addedBlocks
    : // 월(1)~일(0) 10:00~19:00 기본 블록 생성
      [1, 2, 3, 4, 5, 6, 0].map((day) => ({
        day,
        startTime: "10:00",
        endTime: "19:00",
      }));
```

**동작 방식**:
1. 템플릿 블록 세트 생성 시
2. 사용자가 블록을 추가하지 않았을 때 (`addedBlocks.length === 0`)
3. 자동으로 월~일 10:00~19:00 블록 7개 생성

**제한사항**:
- 블록 세트를 생성하는 시점에만 적용됨
- 블록 세트를 생성하지 않고 템플릿을 저장하면 기본값이 생성되지 않음

---

### 2. 캠프 모드에서 블록 세트 처리

#### 위치: `app/(student)/camp/[invitationId]/page.tsx`

**현재 동작**:
- 템플릿에서 `block_set_id`가 설정되어 있으면 해당 블록 세트를 조회합니다.
- 블록 세트나 블록이 없으면 검증 에러를 발생시킵니다.

```typescript:107:139:app/(student)/camp/[invitationId]/page.tsx
if (templateData.block_set_id) {
  // 템플릿 블록 세트 조회
  // ...
  if (!templateBlocks || templateBlocks.length === 0) {
    validationErrors.push(
      "템플릿의 블록 세트에 블록이 없습니다. 관리자에게 문의해주세요."
    );
  }
}
```

**문제점**:
- 템플릿에 `block_set_id`가 없거나 블록이 없으면 에러만 발생
- 기본값 블록 세트를 자동으로 생성하지 않음
- 사용자가 계속 진행할 수 없음

---

### 3. 템플릿 저장 시 블록 세트 검증

#### 위치: `app/(student)/plan/new-group/_components/PlanGroupWizard.tsx`

**현재 동작**:
- 템플릿 모드에서는 블록 세트 검증이 제외됩니다.
- 블록 세트가 없어도 템플릿 저장이 가능합니다.

```typescript:543:548:app/(student)/plan/new-group/_components/PlanGroupWizard.tsx
// 블록 세트: 학생 입력 허용이 아닐 때만 필수
// 템플릿 모드에서는 블록 세트가 없어도 기본값 블록 세트가 자동 생성되므로 필수 아님
if (!isTemplateMode && !isStudentInputAllowed("block_set_id")) {
  if (!wizardData.block_set_id) {
    errors.push("블록 세트를 선택해주세요.");
  }
}
```

**문제점**:
- 주석에는 "기본값 블록 세트가 자동 생성된다"고 되어 있지만, 실제로는 템플릿 저장 시 자동 생성되지 않음
- 템플릿 저장 후 편집 모드에서 수동으로 블록 세트를 생성해야 함

---

## 🔍 문제점 요약

### 1. 템플릿 저장 시 기본값 생성 미구현
- 템플릿 저장 시 `block_set_id`가 없어도 저장 가능
- 하지만 기본값 블록 세트가 자동으로 생성되지 않음
- 나중에 수동으로 블록 세트를 생성해야 함

### 2. 캠프 참여 시 기본값 처리 미구현
- 템플릿에 `block_set_id`가 없거나 블록이 없으면 에러 발생
- 기본값 블록 세트를 자동으로 생성하거나 사용할 수 없음

### 3. 일관성 문제
- 블록 세트 생성 시에는 기본값이 자동 생성됨
- 하지만 템플릿 저장이나 캠프 참여 시에는 기본값 처리 로직이 없음

---

## 💡 개선 방안

### 방안 1: 템플릿 저장 시 기본값 블록 세트 자동 생성

**구현 위치**: `app/(admin)/actions/campTemplateActions.ts`의 `createCampTemplateAction`

**로직**:
1. 템플릿 저장 시 `block_set_id`가 없으면
2. 기본 블록 세트를 자동 생성 (이름: "기본", 블록: 월~일 10:00~19:00)
3. 생성된 블록 세트 ID를 템플릿에 연결

**장점**:
- 템플릿 저장 후 바로 사용 가능
- 사용자 편의성 향상

**단점**:
- 템플릿이 불완전한 상태로 저장되는 것 방지
- 나중에 수정할 수 있음

### 방안 2: 캠프 참여 시 기본값 블록 세트 자동 생성/사용

**구현 위치**: `app/(student)/camp/[invitationId]/page.tsx`

**로직**:
1. 템플릿에 `block_set_id`가 없거나 블록이 없으면
2. 기본 블록 세트를 임시로 생성하거나
3. 기본 블록 정보를 직접 사용 (DB 저장 없이)

**장점**:
- 사용자가 캠프 참여를 계속 진행할 수 있음
- 에러 없이 스케줄 생성 가능

**단점**:
- 템플릿 설정이 불완전한데도 진행 가능
- 나중에 문제가 발생할 수 있음

### 방안 3: 기본값 블록 세트를 별도 유틸리티로 분리

**구현 위치**: `lib/utils/defaultBlockSet.ts` (신규 생성)

**기능**:
- 기본 블록 세트 정보 반환 함수
- 기본 블록 세트 생성 함수
- 여러 곳에서 재사용 가능

**장점**:
- 일관된 기본값 관리
- 유지보수 용이

---

## 📝 권장 방안

**방안 1 + 방안 3 조합**

1. 기본값 블록 세트 유틸리티 생성 (`lib/utils/defaultBlockSet.ts`)
2. 템플릿 저장 시 기본값 블록 세트 자동 생성
3. 캠프 참여 시에도 기본값 블록 세트가 없으면 자동 생성

이렇게 하면:
- 템플릿 저장 후 바로 사용 가능
- 캠프 참여 시 에러 없이 진행 가능
- 일관된 기본값 관리

---

## 🔧 구현 시 고려사항

### 1. 기본 블록 세트 이름
- 현재: "기본"
- 템플릿용: "기본 블록 세트" 또는 "템플릿 기본 블록"

### 2. 기본 블록 시간
- 현재: 월~일 10:00~19:00
- 이 값이 적절한지 검토 필요

### 3. 기본값 덮어쓰기
- 이미 블록 세트가 있으면 기본값을 생성하지 않음
- 사용자가 나중에 수정 가능

### 4. 에러 처리
- 기본값 생성 실패 시 에러 처리
- 사용자에게 알림 제공

---

## 📌 다음 단계

1. [ ] 기본값 블록 세트 유틸리티 생성
2. [ ] 템플릿 저장 시 기본값 블록 세트 자동 생성 로직 추가
3. [ ] 캠프 참여 시 기본값 블록 세트 처리 로직 추가
4. [ ] 테스트 및 검증
5. [ ] UI 설명 텍스트 업데이트 (이미 부분적으로 추가됨)

